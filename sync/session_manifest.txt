# Claude Session Manifest -- 2026-09-19 (afternoon/evening)
# ------------------------------------------------------------
# Lists all src/ files modified by Claude this session.
# Use as import checklist after each Claude session.
#
# Import status key:
#   [IMPORT]   - must be imported into VBA editor (Remove old, Import new)
#   [XML]      - ribbon XML only; run py/inject_ribbon.py (no VBA import)
#   [SCRIPT]   - Python/tooling; no VBA import
#   [DOC]      - documentation only; no import
#   [MEMORY]   - Claude auto-memory; no project import
# ------------------------------------------------------------

## Files touched this session

  [IMPORT] src/basBiblePalette.bas -- added Public Function
  AuditColorConstants() (RUN_THE_TESTS Test 88) + private helper
  LiveCheckAutomaticColorReadback(). Dumps Long/Hex/RGB for the six
  built-in WdColor* constants this codebase actually uses
  (wdColorAutomatic/Black/Red/DarkRed/Blue/DarkBlue) and runs a live
  check of what Font.Color reads back as after being set to
  wdColorAutomatic, via a throwaway Documents.Add(Visible:=False) doc
  (never ActiveDocument).
    STATUS: already imported and run by the operator this session --
    RUN_THE_TESTS(88) PASS, result 6, expected 6. Live check confirmed
    Automatic stays the -16777216 sentinel on readback (does not resolve
    to Black). All six constants exactly match basBiblePalette.bas's
    existing named-color RGB values -- no drift found. Nothing further
    to import for this file.

  [IMPORT] src/aeBibleClass.cls -- wired Test 88 into the four standard
  dispatch locations (MaxTests 87->88, Expected1BasedArray +1 entry
  (expected 6), GetTestDescription/GetPassFail/RunTest/OutputTestReport
  Case 88) plus a thin Private Function AuditColorConstants() wrapper
  delegating to basBiblePalette.AuditColorConstants(). Per
  md/Adding_To_Bible_Test_Class.md's checklist.
    STATUS: already imported and confirmed passing (see above). Nothing
    further to import.

  [IMPORT] src/ThisDocument.cls -- Document_Open now calls new Private
  Sub CheckForConflictingRibbonAddIn, which detects aeRibbon.dotm in
  Application.StartupPath and offers (MsgBox Yes/No) to unload
  (AddIn.Installed = False) and Kill it. Fixes the "2 Radiant Word Bible
  tabs" bug reported this session (root cause: aeRibbon.dotm v1.0.0
  installed globally in Word's Startup folder, on top of this docm's own
  embedded ribbon). All new code lives after line 10 (Option Explicit) --
  verified this does not interfere with
  basImportWordGitFiles.ImportThisDocumentFile's header-skip parsing,
  which starts capturing the body at exactly that line.
    STATUS: already imported and confirmed working live by the operator
    ("CheckForConflictingRibbonAddIn worked as planned") -- successfully
    unloaded and deleted the Startup-folder copy in one pass, no Err 70,
    no Word restart needed. Nothing further to import.

  [DOC] aeRibbon/BUILD.md -- G8 close-out gained step 6: delete the
  Startup-folder copy of aeRibbon.dotm once G7/G8 pass, since it's a
  disposable build artefact regenerated by the build procedure itself,
  not something that should persist between release cycles. Directly
  addresses the root cause above at the source (the release procedure),
  with ThisDocument.cls's new guard as a backstop if this step is ever
  missed.

  [DOC] rvw/Plan_vba_color_constants_for_js_port_2026-09-19.md (new
  file) -- full writeup: RGB/hex reference table (now filled with
  confirmed Test 88 values, all matching basBiblePalette.bas with no
  drift), codebase examination of all six wdColor* call sites (which are
  in scope for the JS port vs. VBA-only tooling), the "RGB keyed to named
  colors" standard (basBiblePalette.bas adopted as SSOT), pros/cons, and
  i18n/cross-platform notes. Bottom line recorded: Cases 39/40/41/43 in
  the aeBibleAddin JS port are unblocked -- hard-code 0/#000000 for Black
  and 128/#800000 for DarkRed; Automatic still needs its own sentinel
  handling on the Office.js side (untested there specifically).

  [DOC] adaept5tudio (separate repo, C:\adaept\adaept5tudio) --
  docs/aeBibleClass-word-addin-conversion-plan.md: §15.11's Cases
  39/40/41/43 row updated from "needs operator input" to unblocked with
  the confirmed values; §15.12 point 1 marked done, pointing at the new
  aeBibleClass plan doc.

## Pre-existing, unrelated drift noticed (not from this session's edits)

  aeBibleClass/rpt/HeadingLog.txt shows as modified in `git status` (66
  lines changed, sessionID timestamp column only -- book names/indices
  unchanged). Regenerated by aeRibbonClass.LogHeadingData as a side
  effect of opening/running the dev docm this session (confirmed
  legitimate test activity: Test 88 run, ribbon-fix verification), not a
  deliberate edit. Left as-is, uncommitted -- same posture as the prior
  session's manifest for this same file.

  [DOC] aeBibleAddin (separate repo, C:\adaept\aeBibleAddin) -- Phase E
  batch 10, commit f2932c1: ported Cases 39/40/43 (CountParagraphMarks_
  ArialBlack/_ArialBlackDarkRed/CountParagraphMarksWithDarkRedFormatting)
  as countParagraphMarksArialBlackLive/ArialBlackDarkRedLive/
  WithDarkRedLive in taskpane/src/word-audit-scanner.ts, reusing the
  already-validated font-color shape extended with .font.name/.font.size.
  Black/DarkRed hex values hard-coded from Test 88's confirmed results.
  Case 41 deliberately NOT ported -- closer reading of its VBA source
  showed it needs per-run character-style attribution within a paragraph,
  the same gap as Cases 82/83/87/7/8, not a simple font-color-shape
  reuse. 11 new tests, 132/132 passing, typecheck clean.
    STATUS: pushed to aeBibleAddin's `main` (f2932c1).

  [DOC] adaept5tudio (separate repo, C:\adaept\adaept5tudio) --
  conversion-plan.md updated: §14 Phase E batch 10 entry added, running
  total 44 -> 47; §15.11's Cases-39/40/41/43 row split (39/40/43 marked
  DONE, 41 moved into the 82/83/87/7/8 character-style-search family
  row); §15.11's pending-live-validation table gained a row for
  AUTOMATIC_FONT_COLOR (asserted from the Word JS API reference, not yet
  live-checked).
    STATUS: pushed to adaept5tudio's `main` (bcc1316).

  [DOC] aeBibleAddin -- Phase E batch 11, commit c1fabbd: ported Case 44
  (CountNonPaletteStyleColors / AuditNonPaletteStyleColors) as
  auditNonPaletteStyleColorsLive, newly unblocked by Test 88 resolving
  its color-constant blocker. New src/data/color-palette.ts (13-entry
  SSOT mirroring basBiblePalette.bas's BuildDefaultPalette). Reuses the
  already-validated Styles-collection shape and objectThemeColor, no new
  shape. 11 new tests, typecheck + lint clean, 139/139 + 45/45 passing.
    STATUS: pushed to aeBibleAddin's `main` (c1fabbd).

  [DOC] adaept5tudio -- conversion-plan.md updated: §14 Phase E batch 11
  entry added, running total 47 -> 48; Case 44 removed from §15.11's
  blocked/deferred tracker (done); Styles-collection and
  AUTOMATIC_FONT_COLOR pending-live-validation rows extended to include
  Case 44.
    STATUS: pushed to adaept5tudio's `main` (e48bd10).

  [DOC] aeBibleAddin -- Shape 5 spike, commit db99c0f: character-style
  search via OOXML parsing (conversion-plan.md §15.13). Re-triaging
  Cases 82/83/87/7/8/41 before building found §15.10 wrong about which
  VBA technique 82/83/87 use -- direct per-character style reads
  (basVerseStructureAudit.bas's Characters(1..12).style.NameLocal), not
  Range.Find.Style (VBA's own comment: Find degenerates on
  character-style runs in this large document, 300-2700s vs seconds).
  New taskpane/src/ooxml-run-styles.ts parses Range.getOoxml() output;
  built on an unverified "flat OPC" package assumption that degrades
  gracefully if wrong. Pilot countChapterVerseMarkerInRangeLive (Case
  83) wired into window.aeAuditSpike, NOT YET LIVE-VALIDATED -- needs
  a testing copy of the docm with real Bible content (not a blank
  sideload doc, since the custom VerseText/marker styles don't exist
  there). Full runbook:
  word-audit-scanner.LIVE_CHECK.md's new "Shape 5" section. Added jsdom
  as a taskpane-only devDependency (test-only). 11 new tests, typecheck
  + lint clean, 154/154 (taskpane) + 45/45 (root) passing. Cases
  82/87/7/8/41 deliberately not built until the pilot's live check
  passes.
    STATUS: pushed to aeBibleAddin's `main` (db99c0f).

  [DOC] adaept5tudio -- conversion-plan.md §15.13 added (Shape 5 spike
  writeup); §15.11's Cases 82/83/87/7/8/41 row updated to point at it.
    STATUS: pushed to adaept5tudio's `main` (ea01ee3).

  [DOC] aeBibleAddin -- Shape 5 live-check PASSED, same session. Operator
  ran the runbook (word-audit-scanner.LIVE_CHECK.md) against a real
  Bible file: dumpSelectionOoxml's output matched the flat-OPC-with-
  inline-styles.xml assumption exactly, including the precise styleId->
  name mappings (ChapterVersemarker->"Chapter Verse marker", Versemarker
  ->"Verse marker"); countChapterVerseMarker() returned 1 for a single
  VerseText paragraph with its Chapter Verse marker, matching
  expectation with zero drift. LIVE_CHECK.md's Shape 5 result log and
  "Result: validated." paragraph updated accordingly.

  [DOC] aeBibleAddin -- Phase E batch 12, same session (not yet
  committed as of this manifest entry): Cases 82/87/7/8 ported on the
  now-validated Shape 5, reusing ooxml-run-styles.ts. New
  hasStyleWithinFirstNChars() helper added there for Cases 82/87
  (mirrors GetMarkerTotals/FindMarkerStyleOutsideVerseText's
  Characters(1..12).style scan exactly, confirmed by reading
  basVerseStructureAudit.bas directly). Cases 7/8 walk every run
  document-wide for text-pattern matches inside character-styled runs.
  Found while porting Case 7: VBA's CountStyleWithSpaceAndNumber has a
  leftover debug early-exit that under-counts (stops after first match)
  -- this port deliberately does NOT replicate that bug. Case 41 still
  not ported (needs rPr font-override parsing, separate design task).
  20 new tests, typecheck + lint clean, 167/167 (taskpane) + 45/45
  (root) passing. adaept5tudio's conversion-plan.md §15.13/§15.11
  updated to match (running total 48 -> 52 of ~87).
    STATUS: pushed to aeBibleAddin's `main` (860c72a) and adaept5tudio's
    `main` (d439f87).

  [DOC] aeBibleAddin -- Phase E batch 13, same session: Case 41
  (Count_ArialBlack8pt_Normal_DarkRed_NotEmphasisRed) ported as
  countArialBlack8ptDarkRedNotEmphasisRedLive, closing the last open
  item from the original Shape 5 group. New runFont() in
  ooxml-run-styles.ts reads a run's DIRECT w:rFonts/w:sz/w:color
  overrides from its own <w:rPr> (not yet live-validated -- new
  sub-shape beyond the w:rStyle resolution the original Shape 5 check
  confirmed; tracked in LIVE_CHECK.md's new "Shape 5, rPr extension"
  section, low priority since Case 41's VBA baseline is 0). Two
  interpretive judgment calls made explicit in the code/doc comments
  rather than silently absorbed: (1) VBA's post-Find
  `Characters(1).style <> "EmphasisRed"` check is not replicated --
  tautological in an exact per-run model, since a run's own style can't
  be both "Words of Jesus" and "EmphasisRed" simultaneously; (2) color
  is checked with inheritance awareness ("Words of Jesus"'s own style
  definition is already DarkRed per the SSOT audit in
  aeBibleClass/rvw/Code_review 2026-05-16.md, so a run with no direct
  color override still satisfies the DarkRed criterion by inheritance).
  11 new tests, typecheck + lint clean, 178/178 (taskpane) + 45/45
  (root) passing. adaept5tudio's conversion-plan.md §15.11/§15.13
  updated (running total 52 -> 53 of ~87; original six-Case Shape 5
  group -- 82/83/87/7/8/41 -- now fully closed).

  [DOC] aeBibleAddin -- live-checked both of §15.11's remaining
  pending-live-validation items this session, operator running
  window.aeAuditSpike hooks against a testing copy of the docm with
  real Bible content:

  1. getStyles()/StyleCollection shape (Cases 5/44/45/76/77/78):
  VALIDATED, but only after finding and fixing THREE genuine bugs the
  mock tests could never catch. (a) A single .load() combining
  items/font/color + items/font/textColor/objectThemeColor across the
  WHOLE unfiltered style collection throws RichApi.Error: That
  property is not available on that object on sync() -- fixed by
  excluding Table/List styles first. (b) Even after that, loading both
  properties TOGETHER in one .load() call on the same filtered
  candidates still threw, though each loaded fine in isolation (proven
  via ad hoc per-property Word.run tests) -- fixed by loading and
  syncing each property in its own round trip. (c) items/
  automaticallyUpdate throws the same error across the whole
  unfiltered collection -- isolated per-property testing confirmed
  it's specifically unsupported for at least one non-Paragraph style;
  fixed by loading it only for the Paragraph-type subset.
  auditNonPaletteStyleColorsLive (Case 44, shipped batch 11) had been
  silently broken against real Word this whole time -- this was the
  first time it was ever run live. Final confirmation: auditApproved
  Styles/countUnapprovedVisibleStyles/auditNonPaletteStyleColors
  matched live VBA RUN_THE_TESTS(5,45,76,77,78,44) byte-for-byte,
  including the one genuine non-zero finding on both sides --
  "Default Paragraph Font" (UnhideWhenUsed=True, Priority=2 expected
  34), a known, deliberate exception per aeBibleClass/rvw/Code_review
  2026-05-16.md, not a port bug.
    STATUS: pushed to aeBibleAddin's `main` (3378173, 70eda7f,
    7401a2e, 107e611).

  2. AUTOMATIC_FONT_COLOR = 'Automatic' (Cases 39/44): REFUTED, not
  validated. Live-check found the opposite of the Word JS API
  reference's suggestion -- an Automatic-colored paragraph's
  .font.color read back as a concrete hex value (#000000), not the
  literal string 'Automatic', and a document-wide count of literal
  'Automatic' matches returned 0 across all ~33,825 body paragraphs.
  Unlike VBA (Test 88 confirmed its object model DOES preserve its own
  -16777216 sentinel on readback), Office.js always resolves Automatic
  to a concrete color. Kept the constant as a documented dead end
  rather than deleting it; removed the two now-dead comparisons
  against it (Cases 39/44) as they changed nothing in practice
  (Automatic resolved to Black here, already caught by each function's
  sibling Black/palette check); updated mock tests accordingly.
    STATUS: pushed to aeBibleAddin's `main` (93dd703).

  [DOC] adaept5tudio -- conversion-plan.md §15.11 updated: both
  pending-live-validation table rows closed (getStyles validated,
  AUTOMATIC_FONT_COLOR refuted). No open live-validation questions
  remain in the Phase 3 port as of this session.
    STATUS: pushed to adaept5tudio's `main` (d4ad1f1, 99ca1fb).

  [DOC] End-of-session doc-consistency audit (fork-verified, read-only
  pass first, then fixes applied). Found and fixed three stale spots in
  adaept5tudio/docs/aeBibleClass-word-addin-conversion-plan.md, none
  from this session's actual work but left over from earlier ones: (1)
  a duplicate hand-maintained running-total paragraph had drifted to
  "29 of ~87" (stopped being updated after batch 3) directly below the
  correct "53 of ~87" in §14's task table -- removed rather than kept
  in sync by hand going forward; (2) Cases 39/40/43's tracker row still
  said AUTOMATIC_FONT_COLOR's Case 39 branch was "flagged NOT
  live-verified," contradicting §15.11's own section stating it was
  refuted and removed; (3) §15.12 "Next steps" was a mid-session
  snapshot (44/~87, "pause here") superseded by batches 10-13 and both
  pending-live-validation closures -- rewritten with current state and
  the actual remaining open API-research items. Also fixed
  aeBibleAddin/taskpane/src/word-audit-scanner.LIVE_CHECK.md's Shape
  2/3/4 section headings, which still said "pending" despite their own
  Result lines already saying "validated" (pre-existing drift from an
  earlier session, not this one).
    STATUS: pushed to adaept5tudio's `main` (cd4a020) and aeBibleAddin's
    `main` (aabb299).

## Session status at this checkpoint

  All work this session is committed and pushed: aeBibleAddin (860c72a,
  31c1cdb, 7dfc65d, 3378173, 70eda7f, 7401a2e, 107e611, 93dd703,
  aabb299), adaept5tudio (d439f87, 5d80694, d4ad1f1, 99ca1fb, cd4a020).
  Working trees clean except aeBibleClass/rpt/HeadingLog.txt (see
  above, left uncommitted deliberately). All docs verified consistent
  as of this checkpoint -- running totals, pending-live-validation
  status, and Shape result logs all agree across conversion-plan.md,
  LIVE_CHECK.md, and memory.

## New session (2026-09-19 continued): §15.12 item 1 research (doc.Shapes, Case 2)

  Picked up conversion-plan.md §15.12's next-steps list, item 1:
  research whether Office.js's Word API supports `doc.Shapes` (floating
  shapes/text boxes with recursive group traversal), the thing Case 2 /
  `CountDoubleSpacesInShapes` needs. §15.11 had this flagged as
  "not confirmed either way."

  [CODE] aeBibleAddin commit `03af4d6`: research resolved better than
  forecast -- `Word.Shape`/`Word.ShapeCollection`/`Word.ShapeGroup`
  exist (API set WordApiDesktop 1.2) and are fully sufficient, no OOXML
  fallback needed. `Shape.type` distinguishes `Group` (recursed via
  `.shapeGroup.shapes`, VBA's `GroupItems` equivalent) from
  `TextBox`/`GeometricShape` (text-bearing) from
  `Picture`/`Canvas`/`Unsupported` (skipped, matching VBA's
  `TextFrame Is Nothing` fallthrough). One real gap found along the
  way: `Shape.textFrame` (`Word.TextFrame`) looked like the obvious
  property to read a shape's text from but only exposes a boolean
  `hasText`, nothing else -- `Shape.body` (a `Word.Body`, "only applies
  to text boxes and geometric shapes" per Microsoft's own docs) is the
  one with `.text`, the same property already validated on
  `Word.Paragraph` elsewhere in this codebase. Ported as
  `countDoubleSpacesInShapesLive` in `word-audit-scanner.ts`. 9 new
  tests (recursion into nested groups included), typecheck clean,
  186/186 (`taskpane`) + 45/45 (root) passing. Flagged as a new
  top-level collection shape pending its own live-check per this
  codebase's established discipline -- new "Shape 6" section added to
  `word-audit-scanner.LIVE_CHECK.md` with the runbook (not yet run;
  Case 2's own VBA baseline is 0, so no urgency).
    STATUS: pushed to aeBibleAddin's `main` (03af4d6).

  [DOC] adaept5tudio commit `0c3ca30`: conversion-plan.md updated --
  §14's task table gets a new Batch 14 entry, running total 53 -> 54 of
  ~87; §15.11's Case 2 row removed from the "open, not investigated"
  tracker and replaced with a "RESOLVED and PORTED, pending
  live-validation" writeup; §15.12's next-steps list renumbered down to
  7 remaining open items (was 8) with a note that item 1's own outcome
  (a full API match, better than the "might be a dead end" forecast)
  is proof these forecasts are genuinely uncertain until checked, not
  just pessimism-by-default.
    STATUS: pushed to adaept5tudio's `main` (0c3ca30).

## Session status at this checkpoint (2026-09-19 continued)

  All work this session is committed and pushed: aeBibleAddin
  (03af4d6), adaept5tudio (0c3ca30). Working tree clean in both.
  aeBibleClass/rpt/HeadingLog.txt remains the one pre-existing
  uncommitted file (left as-is, not touched this session).

## Same session, continued: Shape 6 live-check run and PASSED

  Operator ran the new "Shape 6" runbook (word-audit-scanner.LIVE_CHECK.md)
  against the already-open sideloaded Word session, step by step:
  baseline (no shapes) -> `0`; one text box containing "one  two" (two
  spaces) -> `1`; a second text box ("three  four") added and grouped
  with the first via Group > Group -> `2`. All three matched exactly,
  including the group-recursion case -- the first live confirmation
  anywhere in this port that a self-referential collection shape
  (`Shape.shapeGroup.shapes` nesting back into another
  `Word.ShapeCollection`) actually works against real Word, not just
  the mock.

    [DOC] aeBibleAddin commit `0533c3d`: LIVE_CHECK.md's Shape 6 section
    heading changed to VALIDATED, result log filled in, closing
    paragraph rewritten to "Result: validated."
      STATUS: pushed to aeBibleAddin's `main` (0533c3d).

    [DOC] adaept5tudio commit `71f74a8`: conversion-plan.md's Case 2
    finding upgraded from "RESOLVED and PORTED, pending live-validation"
    to "RESOLVED, PORTED, and VALIDATED"; §15.11's pending-live-validation
    table confirmed empty again; §15.12's next-steps intro updated to
    say no open live-validation questions remain anywhere in the port;
    §14's Batch 14 task-table cell updated to record the live-check
    result inline.
      STATUS: pushed to adaept5tudio's `main` (71f74a8).

## Session status at this checkpoint (2026-09-19, Shape 6 closed)

  All work this session is committed and pushed: aeBibleAddin
  (03af4d6, 0533c3d), adaept5tudio (0c3ca30, 71f74a8). Working tree
  clean in both. aeBibleClass/rpt/HeadingLog.txt remains the one
  pre-existing uncommitted file (left as-is, not touched this session).
  Phase E running total: 54 of ~87, zero open live-validation questions
  remaining anywhere in the port.

## Same session, continued: §15.12 "ListTemplates" item research (Case 75)

  Picked up the next item on §15.12's next-steps list: research whether
  Office.js's Word API supports `ActiveDocument.ListTemplates`
  traversal, the thing Case 75 /
  `CountApprovedStylesWithListParagraphRisk` needs (guards against the
  Word List Paragraph numbering-engine hang, EDSG/10-list-paragraph-
  bug.md). §15.11 had this flagged as "a collection not yet used
  anywhere in this port," no confirmation either way.

  [CODE] aeBibleAddin commit `e14ebf5`: another full API match, same
  outcome as Case 2's `doc.Shapes` research. `context.document.
  listTemplates` (`Word.ListTemplateCollection`, WordApiDesktop 1.4) ->
  each template's `.listLevels` (`Word.ListLevelCollection`,
  WordApiDesktop 1.1) -> each level's `.linkedStyle` (a plain string)
  is the exact same collection shape as VBA's `ActiveDocument.
  ListTemplates -> ListLevels -> LinkedStyle`, so the same
  reverse-lookup-dictionary approach ports directly. `Style.baseStyle`
  (API set WordApi 1.5 -- notably NOT desktop-only, unlike almost
  everything else touched in this port) covers the other half. Ported
  as `countApprovedStylesWithListParagraphRiskLive` in
  `word-audit-scanner.ts`; a violating style counts as 2, matching
  VBA's two independent `violations = violations + 1` increments. 8 new
  tests, typecheck clean, 194/194 (`taskpane`) + 45/45 (root) passing.
  Flagged as a new top-level collection shape pending its own
  live-check per this codebase's established discipline -- new "Shape
  7" section added to `word-audit-scanner.LIVE_CHECK.md`, plus a
  `dumpListTemplateSample` diagnostic hook in `taskpane.ts` (not yet
  run; Case 75's own VBA baseline is 0, so no urgency). Unlike Case 2's
  Shape 6, this runbook can only manufacture a positive test for the
  `baseStyle` half -- the `linkedStyle` half is confirmed only by the
  dump, since the Word UI action that links a list level to a style
  ("Define New List Style" -> "Link level to style") is obscure enough
  to skip manufacturing for this check.
    STATUS: pushed to aeBibleAddin's `main` (e14ebf5).

  [DOC] adaept5tudio commit `cd5377b`: conversion-plan.md updated --
  §14's task table gets a new Batch 15 entry, running total 54 -> 55 of
  ~87; §15.11's Case 75 row removed from the "open, not investigated"
  tracker, replaced with a "RESOLVED and PORTED, pending
  live-validation" writeup, and its pending-live-validation summary
  line updated to point at the new entry; §15.12's next-steps list
  renumbered down to 6 remaining open items (was 7).
    STATUS: pushed to adaept5tudio's `main` (cd5377b).

## Session status at this checkpoint (2026-09-19, Case 75 ported)

  All work this session is committed and pushed: aeBibleAddin
  (03af4d6, 0533c3d, e14ebf5), adaept5tudio (0c3ca30, 71f74a8,
  cd5377b). Working tree clean in both. aeBibleClass/rpt/HeadingLog.txt
  remains the one pre-existing uncommitted file (left as-is, not
  touched this session). Phase E running total: 55 of ~87, one open
  pending-live-validation item (Case 75's `ListTemplateCollection`/
  `baseStyle` shape, "Shape 7" in word-audit-scanner.LIVE_CHECK.md).

## Same session, continued: Shape 7 live-check run, SAFETY CATCH by operator

  Operator ran the new "Shape 7" runbook (word-audit-scanner.LIVE_CHECK.md)
  against the already-open sideloaded Word session (a testing copy of
  the docm with real Bible content, still open from Shape 6's check):

  1. `dumpListTemplateSample()` -- succeeded, no thrown error. 19 list
  templates found (10 single-level, 9 nine-level), 40 approved
  Paragraph-type styles read back. Console initially collapsed the
  nested arrays; `JSON.stringify(await
  aeAuditSpike.dumpListTemplateSample())` got the full values. Genuine
  finding: `baseStyle`/`linkedStyle` both read back as `null` for "no
  value," not the empty string this port's doc comments had assumed --
  the code's existing truthy checks (`if (style.baseStyle)`) already
  handle `null` correctly, so no behavior bug, just a wrong assumption
  in the comments (now corrected). Also found one list template
  (levels 3-9) with genuine non-null `linkedStyle` values ("Heading 3"
  through "Heading 9") -- proves the traversal reads real linked-style
  data, not just an all-null degenerate case. None of those names are
  on the approved-styles list, so correctly no effect on the count.

  2. `countApprovedStylesWithListParagraphRisk()` -- returned `0`,
  matching VBA's own baseline exactly.

  3. **STOPPED before the manufactured `baseStyle`-violation positive
  case (originally steps 4-6 of the runbook).** The operator caught,
  before opening Word's Modify Style dialog on "VerseText" as
  instructed, that this is EXACTLY the action
  `aeBibleClass/EDSG/10-list-paragraph-bug.md` documents as triggering
  Word's List Paragraph numbering-engine hang -- reliably reproducible
  in this ~34,000-paragraph document, recovery only by killing Word
  from Task Manager, unsaved work lost. This is literally the bug Case
  75 exists to guard against. **The runbook itself was wrong to ask for
  this in a document with real content at all** -- it should have
  called for a disposable blank `.docm` per the EDSG doc's own
  migration recipe (the hang risk scales with paragraph count, so a
  testing copy of the ~34,000-paragraph docm is exactly as exposed as
  the live master would be).
  Corrected immediately: did NOT proceed with the style modification;
  updated the runbook, the `countApprovedStylesWithListParagraphRiskLive`
  source comment, and the `dumpListTemplateSample`/
  `countApprovedStylesWithListParagraphRisk` debug-hook comment in
  `taskpane.ts` to warn against this and record the actual (partial)
  result honestly instead of leaving it looking like an untested
  pending item.

    [DOC] aeBibleAddin commit `6251f18`: LIVE_CHECK.md's Shape 7 section
    heading changed to "PARTIALLY VALIDATED," safety correction added
    at the top before the steps, steps 4-6 removed from the "to check
    it" list, result log filled in with the dump + baseline results,
    closing paragraph rewritten as "Result: partially validated."
    Source comment in word-audit-scanner.ts and debug-hook comment in
    taskpane.ts both corrected (null, not empty string; safety warning
    against the Modify Style dialog on this document).
      STATUS: pushed to aeBibleAddin's `main` (6251f18).

    [DOC] adaept5tudio commit `e5f15a4`: conversion-plan.md's Case 75
    finding upgraded from "RESOLVED and PORTED, pending live-validation"
    to "RESOLVED, PORTED, and PARTIALLY VALIDATED" with the full safety
    correction recorded inline; §15.12's next-steps intro updated to
    reflect no fully-open pending-live-validation item remains (Case 75
    confirmed on the data it has actually seen, genuine positive
    baseStyle violation deliberately deferred to a disposable document
    by design, not an oversight); the "pause here" closing paragraph
    updated to say "partially validated" for Case 75 rather than
    implying it's untouched.
      STATUS: pushed to adaept5tudio's `main` (e5f15a4).

## Session status at this checkpoint (2026-09-19, Shape 7 partially validated)

  All work this session is committed and pushed: aeBibleAddin
  (03af4d6, 0533c3d, e14ebf5, 6251f18), adaept5tudio (0c3ca30, 71f74a8,
  cd5377b, e5f15a4). Working tree clean in both. aeBibleClass/
  rpt/HeadingLog.txt remains the one pre-existing uncommitted file
  (left as-is, not touched this session). Phase E running total: 55 of
  ~87. No fully-open pending-live-validation item remains -- Case 75's
  shape is confirmed on real data (dump + zero-violation baseline); the
  one thing never observed live (a genuine positive `baseStyle`
  violation) is deliberately deferred to a disposable document, by
  design, per EDSG/10-list-paragraph-bug.md's own guidance -- not an
  oversight or a corner cut. Two open text boxes from Shape 6's live
  check remain in the testing-copy docm, un-cleaned-up (noted
  but not addressed this session).

## Same session, continued: §15.12 "Body-level hyperlinks" item research (Cases 17/46)

  Picked up the next item on §15.12's next-steps list: research whether
  Office.js exposes a Body-level hyperlinks collection, the thing Cases
  17 (`CountActiveHyperlinks`) and 46 (`AuditBookHyperlinkStyling`)
  were grouped under in §15.11's tracker ("no obvious Office.js
  Body-level hyperlinks collection found yet"). Re-triaged both VBA
  sources before building anything -- same discipline that caught
  §15.10's original mis-grouping of Cases 82/83/87 -- and found the
  shared assumption was wrong for Case 46: its VBA source
  (`AuditBookHyperlinkStyling`) is a pure character-style Find + font
  check, no hyperlinks collection anywhere in it. Only Case 17 actually
  needs one.

  [CODE] aeBibleAddin commit `1f0af4f`: third full API match this
  session (after Cases 2 and 75) -- `Word.Range.getHyperlinkRanges()`
  -> `Word.RangeCollection` exists, API set WordApi 1.3 (notably NOT
  desktop-only, one of the few properties this port has needed that
  isn't). Ported as `countActiveHyperlinksLive`
  (`context.document.body.getRange().getHyperlinkRanges()`), scoped to
  the main body only per this port's established Primary-only/
  main-body-only convention (VBA sums across ALL StoryRanges). New
  top-level collection shape (Shape 8), flagged pending its own
  live-check -- but unlike Case 75's Shape 7, this one is LOW-RISK and
  EASY to manufacture: this document's own editorial rule is zero
  hyperlinks anywhere, so inserting/removing one via Word's ordinary
  Insert > Link UI is a safe, reversible action, nothing like the
  Modify-Style-dialog hang risk from Shape 7.

  Case 46 (`AuditBookHyperlinkStyling`) ported as
  `auditBookHyperlinkStylingLive`, reusing Shape 5's OOXML parser --
  no new shape at all. Extended `ooxml-run-styles.ts`'s `runFont()`
  with a fourth field, `underline` (`w:u/@w:val`), riding on the SAME
  still-pending "Shape 5, rPr extension" live-check Case 41's
  name/size/color parsing already waits on (not a new tracked pending
  item). Inheritance-aware exactly like Case 41: read
  `aeBibleClass/rpt/Styles/style_BookHyperlink.txt` directly and
  confirmed the "BookHyperlink" style's OWN definition already is
  Carlito 9pt DarkBlue (RGB 0,0,128 = `#000080`, cross-checked against
  `../../src/data/color-palette.ts`'s existing SSOT entry) single-
  underline -- so a run with no direct override on a given property is
  treated as satisfying it by inheritance; only an EXPLICIT disagreeing
  override counts as an anomaly. Per-run anomaly count, not
  per-property: VBA accumulates all four checks into one `mismatch`
  string per run and increments `anomalies` ONCE if non-empty, ported
  faithfully as one OR-of-four-mismatches check per run (unlike Case
  75's per-property violation count).

  18 new tests (8 for Case 17's mock, 9 for Case 46's OOXML-fixture
  mock plus one underline-specific `ooxml-run-styles.test.ts` addition,
  1 negative-case underline test), typecheck clean, 207/207
  (`taskpane`) + 45/45 (root) passing.
    STATUS: pushed to aeBibleAddin's `main` (1f0af4f).

  [DOC] adaept5tudio commit `7aeb4bd`: conversion-plan.md updated --
  §14's task table gets a new Batch 16 entry, running total 55 -> 57 of
  ~87 (two Cases in one batch); §15.11's Cases-17/46 row removed from
  the "open, not investigated" tracker; two new finding paragraphs
  added (Case 17 pending live-validation, Case 46 needing none);
  §15.12's next-steps list renumbered down to 5 remaining open items
  (was 6); "three for three this session" noted -- every §15.12 API
  research item attempted this session (doc.Shapes, ListTemplates,
  hyperlinks) turned out to be a full or near-full API match, not a
  dead end.
    STATUS: pushed to adaept5tudio's `main` (7aeb4bd).

## Session status at this checkpoint (2026-09-19, Cases 17/46 ported)

  All work this session is committed and pushed: aeBibleAddin
  (03af4d6, 0533c3d, e14ebf5, 6251f18, 1f0af4f), adaept5tudio (0c3ca30,
  71f74a8, cd5377b, e5f15a4, 7aeb4bd). Working tree clean in both.
  aeBibleClass/rpt/HeadingLog.txt remains the one pre-existing
  uncommitted file (left as-is, not touched this session). Phase E
  running total: 57 of ~87. Two pending-live-validation items remain,
  neither urgent: Case 75's `ListTemplateCollection`/`baseStyle` shape
  (partially validated, "Shape 7") and Case 17's
  `Range.getHyperlinkRanges()` shape (not yet checked at all, "Shape
  8," but low-risk/easy whenever picked up). Two open text boxes from
  Shape 6's live check remain in the testing-copy docm,
  un-cleaned-up (noted but not addressed this session). Remaining
  §15.12 research items (5 left): footnote body-text traversal (34/35),
  page/section-break detection (6/80), superscript Find criterion (79),
  Fields/Bookmarks/InlineShapes + dedup (74), multi-type header/footer
  stories (84).

## Same session, continued: real-world incident -> "Lock Hyperlinks" feature (operator-directed)

  Operator reported a real editing incident (not a §15.12 research
  item): a recent session fixing license/map references (which often
  carry pasted links) applied the BookHyperlink character style by
  hand, but Test 17 (CountActiveHyperlinks) still found active
  hyperlinks -- right-click > Remove Hyperlink fixed the one instance
  found. Operator asked for the root cause examined and a structural
  solution proposed, with the explicit requirement that any fix work
  "across all languages" (i.e. also cover the aeBibleAddin/.docx
  pathway used for translated editions, not just this VBA docm).

  Root cause confirmed by reading src/basTEST_aeBibleTools.bas
  directly: applying a character style and removing a Word.Hyperlink
  object are two completely independent operations. The existing
  LockBookHyperlinks Sub already does both correctly but is
  manual-only ("run when hyperlinks are added or changed") -- nothing
  forces it to run.

  [IMPORT] src/basTEST_aeBibleTools.bas -- LockBookHyperlinks gained
  `Optional silent As Boolean = False`. When True, suppresses both of
  its MsgBox calls (the "style not found" early-exit and the
  completion summary), falling back to Debug.Print only. Manual
  callers unaffected (default False, same MsgBox behavior as before).
    STATUS: NOT YET IMPORTED into the live VBA project -- needs the
    standard Remove-old/Import-new dance (or ImportAllVBAFiles) in the
    VBA editor before Document_BeforeSave below can actually run.
    Compile Project after importing, per the ImportAllVBAFiles-workflow
    gotchas memory.

  [IMPORT] src/ThisDocument.cls -- new Private Sub
  Document_BeforeSave(ByVal SaveAsUI As Boolean, Cancel As Boolean).
  Sums story.Hyperlinks.Count across every StoryRange before every
  save (same cheap measure CountActiveHyperlinks uses); if nonzero,
  calls LockBookHyperlinks silent:=True. An active hyperlink can no
  longer survive into a saved .docm.
    STATUS: NOT YET IMPORTED -- same dance needed as above. Both files
    should be imported together in one pass since Document_BeforeSave
    calls LockBookHyperlinks by its bare (unqualified) name.

  [DOC] EDSG/01-styles.md -- new "Automatic enforcement" subsection
  under the existing hyperlink-styling section: documents both
  pathways (this docm's Document_BeforeSave, and aeBibleAddin's
  taskpane mechanism below) side by side, with the real incident as
  the motivating example, and explains explicitly why Office.js needed
  TWO mechanisms instead of VBA's one (no before-save event exists in
  the Word JavaScript API -- confirmed against Microsoft's docs and an
  open office-js GitHub issue this session).

  [DOC] adaept5tudio commit `53fedea` -- conversion-plan.md gained new
  §15.14 documenting both pathways together (not a numbered
  RUN_THE_TESTS Case port, a new capability).
    STATUS: pushed to adaept5tudio's `main` (53fedea).

  [CODE] aeBibleAddin commit `2b28d40` -- new
  taskpane/src/hyperlink-lock.ts, `lockActiveHyperlinksLive`: reads
  `context.document.hyperlinks` -> `Word.HyperlinkCollection` (API set
  WordApiDesktop 1.3, document-wide -- a different object from Case
  17's main-body-scoped read-only `getHyperlinkRanges()`), restyles
  each hyperlink's range to BookHyperlink, then deletes it. **First
  WRITE/mutation this entire port has needed** -- everything before
  this, across 57 ported Cases, was read-only. Deliberately scoped
  down from VBA's three-step LockBookHyperlinks to just step 2 (unlink)
  -- steps 1/3 (migrate orphaned built-in-Hyperlink-styled runs,
  force-reapply font properties) would need a live "find by character
  style, get a writable Range back" capability Shape 5's OOXML-parsing
  approach deliberately doesn't provide (Case 46's
  auditBookHyperlinkStylingLive already covers that drift separately,
  read-only). Wired into the REAL taskpane UI (index.html/taskpane.ts),
  not just a console hook: a "Lock Hyperlinks" button, plus a silent
  auto-run on every Office.onReady (catches drift from a PREVIOUS
  session only -- cannot catch anything added in the current session
  before save, since Office.js has no before-save hook to run it from;
  the button is the real guarantee). Also added an `aeAuditSpike.
  lockActiveHyperlinks()` console hook for the live-check runbook to
  drive the function directly. 5 new tests, typecheck clean, 210/210
  (`taskpane`) + 45/45 (root) passing, production build clean (53.5kB,
  up from ~50kB for the one new icon + function).
    STATUS: pushed to aeBibleAddin's `main` (2b28d40). New "Shape 8,
    write extension" section added to word-audit-scanner.LIVE_CHECK.md
    with a live-check plan -- NOT YET RUN, flagged higher priority than
    most other pending items in that file since this mutates content
    (a bug here could restyle/unlink something unintended, not just
    misreport a count).

## Session status at this checkpoint (2026-09-19, Lock Hyperlinks built)

  All work this session is committed and pushed: aeBibleAddin
  (03af4d6, 0533c3d, e14ebf5, 6251f18, 1f0af4f, 2b28d40), aeBibleClass
  (b95bd60, this manifest commit pending), adaept5tudio (0c3ca30,
  71f74a8, cd5377b, e5f15a4, 7aeb4bd, 53fedea). Working tree clean in
  aeBibleAddin/adaept5tudio; aeBibleClass has the two VBA source files
  committed to git but NOT YET IMPORTED into the live VBA project (see
  above) -- operator needs to do the Remove-old/Import-new dance (or
  ImportAllVBAFiles) before Document_BeforeSave's auto-lock actually
  runs in Word. aeBibleClass/rpt/HeadingLog.txt remains the one
  pre-existing uncommitted file (left as-is, not touched this session).

  Two live-checks still pending, not yet run: Shape 7 (partially
  validated, Case 75), Shape 8 read half (Case 17, low-risk/easy), and
  now also Shape 8's write extension (Lock Hyperlinks itself,
  higher-priority since it's a mutation). Two open text boxes from
  Shape 6's live check remain in the testing-copy docm,
  un-cleaned-up.

## Same session, continued: risk-framing correction (operator feedback)

  Operator corrected how the Lock Hyperlinks live-check's priority/risk
  was described: prior comments framed a bug there as merely "could
  misreport a count" / "restyle something unintended" -- true but
  understating the actual stakes. This document's target is print
  publication; an active hyperlink surviving into that output is a
  defect the whole rule exists to prevent outright, in every language
  equally (i18n does not relax it), and potentially a security concern
  (a malicious/spoofed URL surviving unnoticed into printed or
  PDF-exported output) -- not a style-consistency nit. Web/mobile
  editions are an explicitly separate future track needing their own,
  more stringent security review later; this fix does not loosen the
  print-target rule in the meantime.

  [DOC] aeBibleAddin commit `ee903e1`: reframed the risk/priority
  language in hyperlink-lock.ts's file header and both spots in
  word-audit-scanner.LIVE_CHECK.md's "Shape 8, write extension"
  section (the mid-section paragraph and the "Result: pending"
  closing line).
    STATUS: pushed to aeBibleAddin's `main` (ee903e1).

  [DOC] adaept5tudio commit `9ac82b2`: same reframing applied to
  conversion-plan.md's §15.14 closing paragraph.
    STATUS: pushed to adaept5tudio's `main` (9ac82b2).

  [DOC] aeBibleClass commit `9fed701`: strengthened
  EDSG/01-styles.md's "Companion rule" Rationale section itself (the
  authoritative source doc) with the same print-defect/security
  framing, plus an explicit note that the future online-edition
  clickability concern is a separate track that doesn't loosen this
  rule now.
    STATUS: pushed to aeBibleClass's `main` (9fed701).

  [MEMORY] Saved feedback_hyperlink_policy_severity.md so this framing
  is applied by default in future sessions touching this rule, in
  either the docm/VBA or `.docx`/JS pathway.

## Session status at this checkpoint (2026-09-19, risk framing corrected)

  All work this session is committed and pushed: aeBibleAddin (03af4d6,
  0533c3d, e14ebf5, 6251f18, 1f0af4f, 2b28d40, ee903e1), aeBibleClass
  (b95bd60, dc78747, 9fed701 -- VBA source changes still NOT YET
  IMPORTED into the live VBA project, see above), adaept5tudio (0c3ca30,
  71f74a8, cd5377b, e5f15a4, 7aeb4bd, 53fedea, 9ac82b2). Working tree
  clean in aeBibleAddin/adaept5tudio; aeBibleClass/rpt/HeadingLog.txt
  remains the one pre-existing uncommitted file (left as-is, not
  touched this session).

## VBA import/export dance DONE (operator, 2026-09-19/20) -- new session picks up here

  Operator ran the standard Remove-old/Import-new (or ImportAllVBAFiles)
  dance and recompiled -- `Document_BeforeSave`'s auto-lock is now live
  in the docm for the first time. Word restarted as part of that process
  (new WINWORD.EXE PID observed).

  Live-check of the JS "Lock Hyperlinks" feature (Shape 8 write
  extension) then started, using the already-open blank scratch
  document from testing. Found a real, reproducible bug on round 1 --
  see the aeBibleAddin-side commit `6aa9bbc` (already recorded/pushed)
  for the full fix. Summary: `lockActiveHyperlinksLive` had no guard for
  a missing `BookHyperlink` style (only exists in a docm that already
  has it defined, e.g. a testing copy of the docm -- not a blank
  scratch doc) and threw an uncaught `RichApi.Error:
  InvalidArgument`, yet the auto-run-on-open call had no error handling
  so this would silently fail as an unhandled promise rejection on every
  task pane open. Fixed with a `getByNameOrNullObject` guard (returns
  `null`, matching VBA's own early-exit) plus try/catch in
  `taskpane.ts`'s `lockHyperlinks()`. Runbook corrected too (it wrongly
  told operators to test in a blank scratch doc).

## VBA-side bug analysis (operator-caught, 2026-09-19/20): missing On Error handlers

  While reviewing the JS-side fix above, operator asked directly:
  "since the auto-run-on-open call has no error handling" -- is that
  VBA or JS? Confirmed JS-only (taskpane.ts's lockHyperlinks/
  Office.onReady). But the operator's follow-up question ("if VBA, I
  have standard error handling -- confirm") prompted checking the VBA
  side too, and found a REAL, separate gap:

  - `ThisDocument.Document_BeforeSave` (added this session, b95bd60)
    has **no `On Error GoTo PROC_ERR` at all** -- unlike
    `CheckForConflictingRibbonAddIn`, a few lines below it in the SAME
    file, which has the full PROC_ERR/Debug.Print pattern. Confirmed by
    reading the current file directly.
  - `basTEST_aeBibleTools.LockBookHyperlinks` (also touched this
    session, gained the `silent` parameter) has only a NARROW
    `On Error Resume Next` / `On Error GoTo 0` bracket around the
    style-existence check -- no top-level `On Error GoTo PROC_ERR` for
    the rest of the Sub (the three Find/Hyperlinks loops, the
    `ColorFromName` call). Confirmed by grepping every `On Error`/
    `PROC_ERR` occurrence in the file and reading the Sub directly --
    genuinely missing, not an oversight in reading.

  **Why this is worse in VBA than the JS gap it was found alongside:**
  an unhandled runtime error inside `Document_BeforeSave` -- a real
  Word Application event, not an async fire-and-forget call like the
  JS side -- can surface as a disruptive runtime-error dialog in the
  MIDDLE of an actual Save operation, not just a silently-logged async
  failure. This is inconsistent with this codebase's own established
  convention (`On Error GoTo PROC_ERR` + `Debug.Print`, confirmed
  present in effectively every other routine checked this session:
  `CheckForConflictingRibbonAddIn`, `CountActiveHyperlinks`,
  `AuditBookHyperlinkStyling`, etc.) -- this was a real omission on the
  same-session VBA additions, not a pre-existing project-wide gap being
  newly discovered elsewhere.

  **Planned fix (about to be applied, same session):**
  1. `Document_BeforeSave` gets `On Error GoTo PROC_ERR` at the top and
     a `PROC_EXIT`/`PROC_ERR` block at the bottom, `Debug.Print` only
     (no `MsgBox` -- this handler must stay silent, same reasoning as
     `silent:=True`'s own design intent: never interrupt a Save). On
     error, the hyperlink auto-lock is simply skipped for that save;
     the save itself is NOT cancelled (`Cancel` is never touched).
  2. `LockBookHyperlinks` gets a real `On Error GoTo PROC_ERR` at the
     top (re-established via `On Error GoTo PROC_ERR` immediately after
     the existing narrow bracket's `On Error GoTo 0`, matching the
     exact idiom already used elsewhere in this same file --
     `OptimizedListFontsInDocument`'s `On Error GoTo 0` / `On Error GoTo
     PROC_ERR` pair -- confirmed by reading that Sub directly) and a
     `PROC_EXIT`/`PROC_ERR` block at the bottom, `Debug.Print` only
     (matching this project's "errors to Immediate window, not MsgBox"
     convention for touched handlers -- `PROC_ERR` must not itself pop
     a `MsgBox`, since that would reintroduce the exact
     interrupt-a-silent-auto-run risk this whole fix exists to close).

  Both fixes applied, committed, and pushed: `aeBibleClass` commit
  `5bb1597` (`ThisDocument.cls` + `basTEST_aeBibleTools.bas`).

===============================================================================
CHECKPOINT (2026-09-20) -- SOLID BASE TO RESUME FROM IN A NEW SESSION.
Read this whole block before doing anything else on this thread.
===============================================================================

WHAT HAPPENED, IN ORDER:
1. Operator did the FIRST import/export dance this session (for the
   original `silent:=` + `Document_BeforeSave` changes, commit
   `b95bd60`) -- Word restarted as part of it.
2. Live-check of the JS "Lock Hyperlinks" feature started against a
   blank scratch doc (per the runbook's own, then-wrong, instructions)
   and found a real bug: `lockActiveHyperlinksLive` threw uncaught on a
   missing `BookHyperlink` style. Fixed JS-side (commit `6aa9bbc`,
   already live via the JS dev server -- no VBA import needed for this
   part, `aeBibleAddin` is a separate repo/runtime).
3. Operator asked whether an unrelated risk-framing comment ("no error
   handling") was VBA or JS -- confirmed JS -- but that question
   surfaced a SEPARATE, real VBA gap: `Document_BeforeSave` and
   `LockBookHyperlinks` (both touched in step 1's commit) were missing
   this codebase's standard `On Error GoTo PROC_ERR` handler. Bug
   analysis documented (commit `c477970`), then fixed (commit
   `5bb1597`, both files above).
4. Operator corrected a factual error running through this whole
   session: every "production Bible file/document/docm" reference was
   wrong -- all live-checks (this session and the prior one) were run
   against **a testing copy of the docm**, never the live/master file.
   Corrected everywhere: this manifest, `conversion-plan.md`,
   `word-audit-scanner.LIVE_CHECK.md`, and three memory files.
5. Operator did the SECOND import/export dance (Round 2, commit
   `5bb1597`'s `On Error` fix) + Compile Project -- **CONFIRMED DONE.**
6. Self-correction while writing this checkpoint: an earlier draft of
   this same checkpoint claimed Step 0 of the JS-side live-check
   (blank-doc guard) had "ALREADY PASSED" -- that was never actually
   true. It was asked for, but the operator's next message answered a
   different question instead of reporting the console result. Fixed
   below -- treat Step 0 as NOT YET CONFIRMED.

CURRENT STATE (accurate as of this checkpoint):
- `aeBibleAddin` (JS/taskpane): fully fixed, pushed, and ALREADY LIVE
  via the running dev server (`npm start`) -- no import needed, just
  reload the task pane if it's still open from before the fix landed.
- `aeBibleClass` (VBA/docm): BOTH rounds of source changes
  (`b95bd60`, `5bb1597`) are now imported and compiled in the live VBA
  project, per operator confirmation. **No VBA import action
  outstanding.**
- Neither the VBA-side Document_BeforeSave auto-lock NOR the JS-side
  Shape 8 write-extension (`lockActiveHyperlinks`) has been
  successfully live-validated end-to-end yet. Round 1 of the JS
  live-check found and fixed a real bug (see step 2 above); no
  live-check has been re-run since the fix landed.

NEXT ACTIONS FOR A NEW SESSION, IN ORDER:
1. **VBA-side validation (not yet done at all):** open the (now
   correctly recompiled) testing-copy docm, insert a real hyperlink in
   the body, then **use Word's actual Save command** (not just
   RUN_THE_TESTS) -- confirm (a) no error dialog interrupts the save,
   (b) the Immediate window shows `Document_BeforeSave: 1 active
   hyperlink(s) found before save -- auto-running LockBookHyperlinks.`
   followed by `LockBookHyperlinks complete: ...`, (c) the hyperlink is
   gone (no longer clickable, restyled to BookHyperlink) after the save
   completes, (d) `RUN_THE_TESTS(17)` (`CountActiveHyperlinks`) reads
   `0` afterward.
2. **JS-side validation (Shape 8 write extension, start fresh -- Step 0
   was never actually confirmed, do not skip it):** follow
   `word-audit-scanner.LIVE_CHECK.md`'s "Shape 8, write extension"
   section top to bottom: Step 0 first (a genuinely blank document,
   expect `null` from `aeAuditSpike.lockActiveHyperlinks()`, no thrown
   error), THEN steps 1-7 against a testing copy of the docm that
   actually has `BookHyperlink` defined (NOT a truly blank document).
3. Once both are confirmed, update `word-audit-scanner.LIVE_CHECK.md`'s
   result log (round 2) and `conversion-plan.md`'s §15.14 entry from
   "bug found and fixed, re-validation pending" to "validated," and
   record the VBA-side Document_BeforeSave live-check result somewhere
   (this manifest, at minimum -- no dedicated VBA-side LIVE_CHECK.md
   equivalent exists in this project; RUN_THE_TESTS + this manifest is
   the VBA-side validation ritual).

NOT BLOCKING THE ABOVE, LOWER PRIORITY, CARRIED FROM EARLIER THIS
SESSION: Shape 7 (`ListTemplateCollection`/`baseStyle`, partially
validated, no further action required unless a genuine need for the
untested `baseStyle`-violation positive case comes up -- test only in
a disposable blank `.docm` per its own runbook, never a document with
real content); 5 remaining §15.12 API-research items (footnote
body-text traversal 34/35, page/section-break detection 6/80,
superscript Find criterion 79, Fields/Bookmarks/InlineShapes + dedup
74, multi-type header/footer stories 84); two leftover test text boxes
in the testing-copy docm from the Shape 6 live-check, un-cleaned-up.

Two things still outstanding from EARLIER in this session, unrelated
to hyperlinks, not blocking the above: Shape 7 (`ListTemplateCollection`/
`baseStyle`, partially validated) and two leftover test text boxes in
the testing-copy docm from the Shape 6 live-check, still
un-cleaned-up.
===============================================================================

## Correction (2026-09-20): every "production Bible file/document/docm" reference above was wrong

  Operator corrected: all of this thread's live-checks (getStyles/
  AUTOMATIC_FONT_COLOR from an earlier session, and this session's
  Shape 5 pilot, Shape 6, Shape 7, and the hyperlink-lock work) were
  run against **a testing copy of the docm**, never the live/master
  file. Every "the real Bible file"/"production Bible document"/
  "production docm"/"production file" reference earlier in this
  manifest has been corrected in place to "a testing copy of the docm"
  (or equivalent) -- the substantive technical findings in each entry
  are unaffected (the testing copy carries the same real content/
  styles/paragraph count, so e.g. Shape 7's List-Paragraph-hang risk
  applies equally there), only the file-identity label was wrong.
  Leftover artifacts from testing (two text boxes, per Shape 6) are in
  that testing copy, not the master docm -- no cleanup of the live file
  is needed on that account.

===============================================================================
# Claude Session Manifest -- 2026-09-20
# ------------------------------------------------------------

## Files touched this session

  [DONE] aeBibleClass/src/ThisDocument.cls -- trimmed 2 stray trailing
  blank lines that had accumulated. Confirmed via commit-history
  comparison this was NOT a recurrence of the 4d603cf per-cycle growth
  bug (re-exporting produced no further growth -- stable). Separately,
  a re-import this session found basTEST_aeBibleTools.bas missing from
  the live VBA project (a prior partial import had skipped it, not a
  code bug) -- LockBookHyperlinks was unresolvable, "Sub or Function
  not defined" on compile. Fixed by re-running the full ImportAllVBAFiles
  dance; module reappeared, compile clean.
    STATUS: imported, compiled clean, committed (3ee3222), pushed.

  [DOC] aeBibleClass/EDSG/01-styles.md -- new subsection "Word feature
  to know: character styles bleed into new typing after Enter", found
  during the aeBibleAddin Shape 8 live-check (typing after a
  BookHyperlink-styled run inherits that character style -- standard
  Word behavior, not a bug in LockBookHyperlinks/lockActiveHyperlinks).
    STATUS: committed (0bbca68), pushed.

  [DOC] aeBibleClass/rpt/HeadingLog.txt -- routine regenerated
  test-run timestamps from this session's compile/import checks.
    STATUS: committed (3ee3222), pushed.

  [SCRIPT] aeBibleAddin/taskpane/src/taskpane.ts -- Office.onReady
  reordered so `lockHyperlinks(false)` runs AFTER `refreshHeadings()`
  completes (`void refreshHeadings().then(() => lockHyperlinks(false))`).
  Fixes a status-message race found during Shape 8's live-check step 7:
  both functions write to the shared #status element, and
  refreshHeadings's own final setStatus (clearing to '' once its
  full-document heading scan completes) was clobbering the "Locked
  N..." message almost as soon as it appeared -- not a timed toast,
  a genuine unsequenced-write race.
    STATUS: typecheck clean, 212/212 tests passing, committed (ad5f543),
    pushed. NOT YET RE-VERIFIED LIVE IN WORD -- do this first next
    session (see below).

  [DOC] aeBibleAddin/taskpane/src/word-audit-scanner.LIVE_CHECK.md --
  recorded the full Shape 8 write-extension round-2 result log (steps
  0, 3-7, all passed against a testing copy with BookHyperlink defined).
  Fixed the "status bar" wording to clarify it means the taskpane's own
  #status element, not Word's window status bar. Closed the Shape 8
  entry as fully validated, no drift.
    STATUS: committed (ad5f543), pushed.

## 2026-09-21: status-message race fix re-verified live -- CONFIRMED

  Operator re-ran the JS-side check: sideloaded the taskpane against a
  testing copy with BookHyperlink defined, inserted a hyperlink,
  closed/reopened the taskpane so Office.onReady fired fresh. Status
  message now persists correctly (no longer clobbered by
  refreshHeadings()'s own status write): "Locked 1 active hyperlink to
  the BookHyperlink style -- no clickable links allowed in this
  document." -- matches taskpane.ts:437 exactly, confirming the
  Office.onReady sequencing fix (commit ad5f543) works live.

    [DOC] aeBibleAddin: word-audit-scanner.LIVE_CHECK.md's Shape 8
    write-extension result log gained a round-2 step-7 correction (was
    wrongly marked "Yes" -- actually a partial/race finding) plus a new
    round-3 re-verification row, closing paragraph updated to
    "closed 2026-09-21."
    [DOC] adaept5tudio: conversion-plan.md's §15.14 gained a
    "Live-check status" closing paragraph -- JS side fully validated
    and closed; VBA side still pending.

  JS-side "Lock Hyperlinks" work (Shape 8, both read and write) is now
  FULLY CLOSED, no open items.

## 2026-09-21: VBA-side Document_BeforeSave validation -- found broken, fixed, CONFIRMED PASSING

  Set out to do the routine live-validation of `Document_BeforeSave`
  (insert a hyperlink, Save, check Immediate window). It never fired --
  not once, across many retries. Long investigation ruled out, in order:
  wrong active document, unsaved changes lost on close, AutoSave/OneDrive
  interference, and file-specific VBA-project corruption (reproduced on
  a completely fresh testing copy imported straight from master, not
  just the session's heavily-reused "Copy (5)" file).

  Root cause, confirmed via the VBA Object Browser (F2, searched
  "BeforeSave"): **Word's `Document` class has no `BeforeSave` event at
  all.** Only `Word.Application.DocumentBeforeSave` exists. A
  same-named `Private Sub Document_BeforeSave(...)` directly in
  `ThisDocument` compiles cleanly, matches the exact signature shown in
  countless online examples, and even appears to a casual glance in the
  Object Browser results -- but is completely inert, never wired to
  anything by Word. **This means the original 2026-09-19/20 "confirmed
  working live" status for this feature was never actually true** -- it
  was never genuinely tested end-to-end with a real Save until this
  session; whatever the operator observed before was not this handler
  firing.

  [IMPORT] src/ThisDocument.cls -- replaced the dead `Document_BeforeSave`
  Sub with a working Application-level hookup: module-level
  `Public WithEvents oWordApp As Word.Application`, wired up in
  `Document_Open` (`Set oWordApp = Application`), handled by
  `oWordApp_DocumentBeforeSave(ByVal Doc As Document, ByRef SaveAsUI As
  Boolean, ByRef Cancel As Boolean)` -- guarded by `If Not Doc Is Me Then
  Exit Sub` since Application-level events fire for every document saved
  in the Word session, not just this one. No new class module needed --
  `ThisDocument` already is one, and `WithEvents` can be declared in any
  class module. Same diagnostic Debug.Print lines and hyperlink-count/
  LockBookHyperlinks logic as the original attempt, just correctly
  wired this time.
    STATUS: imported, compiled clean, and LIVE-VALIDATED by the operator
    on a testing copy:
      oWordApp_DocumentBeforeSave: fired.
      oWordApp_DocumentBeforeSave: activeCount = 1
      oWordApp_DocumentBeforeSave: 1 active hyperlink(s) found before
        save -- auto-running LockBookHyperlinks.
      LockBookHyperlinks complete:
        Migrated from built-in Hyperlink : 1
        Unlinked active Hyperlinks       : 1
        BookHyperlink runs force-locked  : 25
    Hyperlink confirmed gone/restyled (no longer clickable on hover).
    RUN_THE_TESTS(17) confirmed PASS, result 0, expected 0.

  **Known tradeoff, not a bug:** the full three-step `LockBookHyperlinks`
  Find-scan across this ~34,000-paragraph document took **over a
  minute** to run synchronously inside the Save. Word is unresponsive
  for that whole window whenever an active hyperlink is present at save
  time. Documented in EDSG/01-styles.md so it isn't mistaken for a hang.

  Also encountered and fixed along the way: a full `ImportAllVBAFiles`
  run reported "Skipped: 4" instead of the expected "Skipped: 1" --
  `basBiblePalette`, `basTEST_aeBibleTools`, and `Module1` had survived
  `DeleteAllModulesExceptImporter`'s delete pass (likely a VBIDE
  `VBComponents` collection staleness/timing issue right after a mass
  delete) and were then wrongly skipped as "already exists" by
  `ModuleOrClassExists`, silently leaving the live project without them
  entirely -- confirmed missing from the Project Explorer, not just a
  cosmetic miscount. Recovered by manually re-importing the three files
  individually via VBE File > Import File, then Compile Project. This
  is the same failure mode noted on 2026-09-20 for `basTEST_aeBibleTools`
  alone ("a prior partial import had skipped it") -- recurring, not a
  one-off, and worth remembering as a standing risk of the standard
  `ImportAllVBAFiles` workflow: **always check the Skipped count is
  exactly 1 after every full reimport**, not just that the summary
  MsgBox appeared.

  [DOC] EDSG/01-styles.md's "Automatic enforcement" section updated with
  the full corrected implementation, the root-cause finding, and the
  performance tradeoff note.
  [DOC] adaept5tudio conversion-plan.md's §15.14 updated to match.

  Both hyperlink-lock pathways (docm/VBA and `.docx`/JS) are now FULLY
  VALIDATED. No open items remain in this thread.

## 2026-09-21, continued: LockBookHyperlinks performance fix + new Test 89

  The just-confirmed >1 minute Save delay (LockBookHyperlinks' full
  three-step sweep running synchronously) was investigated further at
  the operator's request rather than left as an accepted tradeoff.

  Root cause: steps 1 (migrate built-in Hyperlink-styled runs to
  BookHyperlink) and 3 (force-reapply font/color/underline on
  BookHyperlink runs) each run a Find-by-character-style scan across
  every StoryRange -- the same anti-pattern basVerseStructureAudit.bas
  documents elsewhere as "300-2700s vs seconds" on this document's
  size. Step 2 (unlink -- the only step affecting RUN_THE_TESTS slot 17,
  CountActiveHyperlinks) uses the Hyperlinks collection directly, no
  text search, fast on its own.

  Before making steps 1/3 opt-in, checked whether doing so would reopen
  a RUN_THE_TESTS blind spot (operator's framing: "RUN_THE_TESTS is the
  gateway for acceptance"):
    - Step 3's concern (font/size/color/underline drift on existing
      BookHyperlink runs): ALREADY independently covered by
      AuditBookHyperlinkStyling, wired at slot 46 -- will FAIL if this
      drift accumulates, regardless of whether LockBookHyperlinks
      itself fixes it. No gap.
    - Step 1's concern (a run still carrying the built-in "Hyperlink"
      character style, independent of whether it still has a clickable
      link): confirmed NO existing coverage. Test 17
      (CountActiveHyperlinks) only checks the Hyperlinks collection
      (actual clickable objects), not character style. Test 45
      (CountUnapprovedVisibleStyles) only checks style DEFINITION
      visibility in the Styles gallery (ActiveDocument.Styles), not
      actual per-run text usage in the document body. A real gap --
      confirmed by reading both functions directly, not assumed.

  [IMPORT] src/basTEST_aeBibleTools.bas -- LockBookHyperlinks gained
  `Optional ByVal fullSweep As Boolean = False`. Steps 1 and 3 now
  gated `If fullSweep Then`; step 2 always runs. Completion
  Debug.Print/MsgBox now notes which mode ran ("fast path (unlink-only;
  pass fullSweep:=True for the full sweep)" vs "full sweep (migrate +
  unlink + force-lock)").

  [IMPORT] src/ThisDocument.cls -- oWordApp_DocumentBeforeSave's own
  comment updated to reflect the new default; the actual call
  (`LockBookHyperlinks silent:=True`) is unchanged, since fullSweep
  already defaults to False -- the auto-save hook gets the fast path
  automatically, no code change needed there beyond the comment.

  [IMPORT] src/aeBibleClass.cls -- new Test 89, CountBuiltInHyperlinkStyleRuns
  (Public Function, same Find-by-style technique as AuditBookHyperlinkStyling
  immediately above it), wired into all four standard dispatch locations
  (MaxTests 88->89, Expected1BasedArray +1 entry (expected 0),
  GetTestDescription/GetPassFail/RunTest/OutputTestReport Case 89,
  RunTest(89) added to the sequence after RunTest(88)) per
  md/Adding_To_Bible_Test_Class.md's checklist. Closes the Step-1
  blind spot found above -- expected 0, FAILs if any run anywhere still
  carries the built-in "Hyperlink" style.

  [IMPORT] src/basStyleInspector.bas -- CountBuiltInHyperlinkStyleRuns
  delegate stub added (mirrors AuditBookHyperlinkStyling's stub
  immediately above it), so `?CountBuiltInHyperlinkStyleRuns` resolves
  from the Immediate Window without manually instantiating aeBibleClass.

    STATUS: imported and LIVE-VALIDATED. Real hyperlink inserted,
    RUN_THE_TESTS(17) confirmed FAIL (1) first, then Save triggered:
      oWordApp_DocumentBeforeSave: fired.
      oWordApp_DocumentBeforeSave: activeCount = 1
      oWordApp_DocumentBeforeSave: 1 active hyperlink(s) found before
        save -- auto-running LockBookHyperlinks.
      LockBookHyperlinks complete [fast path (unlink-only; pass
        fullSweep:=True for the full sweep)]:
        Migrated from built-in Hyperlink : 0
        Unlinked active Hyperlinks       : 1
        BookHyperlink runs force-locked  : 0
    RUN_THE_TESTS(17) re-run: PASS (0). No save delay this time (unlike
    the original >1 minute) -- fast path confirmed working as designed.
    RUN_THE_TESTS(89) (CountBuiltInHyperlinkStyleRuns) not yet
    separately exercised this session but wired and compiling clean.

## Bug found + fixed during this import: ImportThisDocumentFile injected an invalid Attribute line

  While importing the fullSweep/Test-89 change above, operator hit a
  compile error (red/error line in the VBE) on
  `Attribute oWordApp.VB_VarHelpID = -1` -- a line Word's own export
  process auto-generates immediately after any WithEvents variable
  declaration (this is the first WithEvents variable ThisDocument has
  ever had, so this case was never hit before this session).

  Root cause: `ImportThisDocumentFile` (basImportWordGitFiles.bas) only
  stripped Attribute/VERSION/BEGIN/END lines from the LEADING header
  block before `Option Explicit`, then accumulated every subsequent line
  verbatim -- including this later, mid-file Attribute line -- into the
  string fed to `CodeModule.AddFromString`. Attribute lines are always
  VBE-managed metadata, never valid inline code; injecting one literally
  is a syntax error, unlike a true `VBComponents.Import` (used for every
  OTHER module), which parses and applies Attribute lines specially
  rather than treating them as code text.

  [IMPORT] src/basImportWordGitFiles.bas -- ImportThisDocumentFile's
  line-filtering loop now skips ANY line starting with "Attribute "
  (case-insensitive), not just ones before the body starts.
    STATUS: imported and confirmed working -- this module is the ONE
    file ImportAllVBAFiles can never refresh itself (DeleteAllModules-
    ExceptImporter explicitly protects it from its own delete pass,
    since a running module can't remove itself -- this is also why it's
    always the expected "Skipped: 1" baseline). Fixing it always
    requires a manual step: either a direct code-pane edit or a
    standalone Remove/Import of just this one file via the VBE, done
    outside the normal ImportAllVBAFiles flow. Confirmed done this
    session -- ThisDocument.cls then reimported cleanly with the fixed
    logic, no more red line, compiled clean.
  src/ThisDocument.cls on disk is unaffected/correct as-is -- the
  Attribute line belongs there as a legitimate export artifact; the bug
  was entirely in how the importer consumed it, not in the source file
  itself.

  [DOC] EDSG/01-styles.md's "Automatic enforcement" section and
  adaept5tudio conversion-plan.md's §15.14 both updated with the full
  research writeup (root cause, the two-test-coverage check, the fix).

## Casing drift found mid-session, root cause confirmed, not yet fully resolved

  A live re-export during the missing-modules investigation showed
  `ImportAllVBAFiles` reporting "Skipped: 4" instead of the expected
  "Skipped: 1" (see the missing-modules section above) -- separately,
  re-exporting individual files after this session's ThisDocument.cls
  edits showed a project-wide `doc` -> `Doc` local-variable casing drift
  across many files, most likely because `oWordApp_DocumentBeforeSave`'s
  own `ByVal Doc As Document` parameter introduced "Doc" as VBA's
  project-wide canonical casing for that identifier, and a full Compile
  Project then re-cased every other module's local `doc` variables to
  match. `py/normalize_vba.py` (existing casing-correction tool, run
  after every export per its own header comment) gained a new rule for
  this. Confirmed as PURE casing drift (no semantic changes) in the 9
  files git already showed modified, but applying the new rule also
  touched 11 MORE files not yet re-exported this session -- unresolved
  whether the live project's Compile Project genuinely re-cased all 39
  modules (in which case this is correct and forward-looking) or only
  the ones actually recompiled/reimported (in which case those 11 files
  now have speculative, unverified changes).

  RESOLVED same session: operator ran a fresh, deliberate full
  reimport/compile/export cycle. Result: the EXACT SAME 24-file diff
  reproduced (same files, same line counts) as before that cycle --
  confirms this is the live VBA project's genuine, stable state after a
  full compile, not a stale/partial artifact from earlier exports. Also
  re-verified the diff content itself with a corrected, properly
  word-boundary-anchored check (the earlier spot-check's grep pattern
  had a bug -- `doc\b` without a leading `\b` also matched "Doc" inside
  "oDoc", silently hiding real diff lines in files using that variable
  name). With the fix, confirmed clean: every remaining diff across all
  24 files is either pure `doc`->`Doc` casing or this session's own
  intentional LockBookHyperlinks/Test-89 additions, nothing else. Safe
  to commit.

STILL LOWER PRIORITY, CARRIED FORWARD UNCHANGED FROM EARLIER SESSIONS:
Shape 7 (`ListTemplateCollection`/`baseStyle`, partially validated, no
further action needed unless the untested `baseStyle`-violation
positive case becomes relevant -- disposable blank `.docm` only, per
its own runbook); 5 remaining §15.12 API-research items (footnote
body-text traversal 34/35, page/section-break detection 6/80,
superscript Find criterion 79, Fields/Bookmarks/InlineShapes + dedup
74, multi-type header/footer stories 84); two leftover test text boxes
in the testing-copy docm from the Shape 6 live-check, still
un-cleaned-up.

===============================================================================
SESSION CLOSE CHECKPOINT (2026-09-21)
===============================================================================

ALL WORK THIS SESSION LIVE-VALIDATED AND DOCS VERIFIED CONSISTENT. Three
repos, all clean and ready to commit/push at time of writing:

  aeBibleClass:      25 files (EDSG/01-styles.md, py/normalize_vba.py,
                      rpt/HeadingLog.txt, sync/session_manifest.txt, and
                      21 src/ files -- see `git status` for the exact list)
  aeBibleAddin:      taskpane/src/word-audit-scanner.LIVE_CHECK.md
  adaept5tudio:      docs/aeBibleClass-word-addin-conversion-plan.md

WHAT CLOSED THIS SESSION:
  1. JS-side "Lock Hyperlinks" status-message race -- re-verified live,
     fix confirmed persisting correctly.
  2. VBA-side Document_BeforeSave -- found to have NEVER actually worked
     (Word's Document class has no BeforeSave event; only
     Word.Application.DocumentBeforeSave does), root-caused via the VBA
     Object Browser, fixed with an Application-level WithEvents hookup
     (oWordApp_DocumentBeforeSave), live-validated end-to-end.
  3. LockBookHyperlinks performance (>1 minute save delay) -- root
     caused to steps 1/3's Find-by-style scans, fixed with a
     fullSweep:=False fast-path default, gap-checked against
     RUN_THE_TESTS coverage (new Test 89 added to close the one real
     gap found), live-validated end-to-end.
  4. Two ImportAllVBAFiles bugs found and fixed along the way: silent
     module-drop on delete (Skipped count gotcha) and an invalid
     mid-file Attribute-line injection bug in ImportThisDocumentFile.
  5. Project-wide `doc`->`Doc` casing drift investigated, confirmed
     genuine (not speculative), normalizer script updated to match.

  Both hyperlink-lock pathways (docm/VBA and .docx/JS), the whole
  Phase 3 audit-engine port's live-validation work, and this session's
  own new work are now FULLY CLOSED. No open items remain in any of
  those threads.

NEXT SESSION, IN ORDER (all lower-priority/optional, nothing blocking):
  1. Consider wiring a ribbon button to `LockBookHyperlinks
     fullSweep:=True` -- currently Immediate-Window-only, no
     discoverable UI entry point for the periodic full-hygiene sweep.
  2. Clean up the two leftover test text boxes in the testing-copy docm
     from the Shape 6 live-check (2026-09-19), still un-cleaned-up.
  3. Shape 7 (`ListTemplateCollection`/`baseStyle`) -- partially
     validated, only needs further action if the untested
     `baseStyle`-violation positive case becomes relevant (disposable
     blank `.docm` only, per its own runbook -- never a document with
     real content, see EDSG/10-list-paragraph-bug.md).
  4. 5 remaining aeBibleAddin §15.12 API-research items (footnote
     body-text traversal 34/35, page/section-break detection 6/80,
     superscript Find criterion 79, Fields/Bookmarks/InlineShapes +
     dedup 74, multi-type header/footer stories 84) -- Phase E JS port
     continuation, running total 57/~87, none blocking.
===============================================================================

===============================================================================
SESSION CLOSE CHECKPOINT (2026-09-21, continued -- worked the prior checkpoint's
"NEXT SESSION" punch list)
===============================================================================

This session did NOT touch aeBibleClass/src/ at all -- no VBA import needed.
Two repos changed, both clean and ready to commit/push at time of writing:

  aeBibleAddin:      taskpane/src/taskpane.ts, word-audit-scanner.ts,
                      word-audit-scanner.test.ts, word-audit-scanner.LIVE_CHECK.md
  adaept5tudio:      docs/aeBibleClass-word-addin-conversion-plan.md

WORKED THROUGH THE PRIOR CHECKPOINT'S 4-ITEM LIST, IN ORDER:

  1. Ribbon button for `LockBookHyperlinks fullSweep:=True` -- CONFIRMED NOT
     NEEDED, closed with no code change. `EDSG/01-styles.md`'s "Automatic
     enforcement" section already documents why: both halves of what
     fullSweep used to auto-fix are independently caught by the normal
     RUN_THE_TESTS gate (test 46 pre-existing, test 89 added the same
     session fullSweep shipped) -- fixing a RUN_THE_TESTS failure via the
     Immediate Window is this codebase's standard remediation pattern
     everywhere else, so a dedicated button would only wrap something
     already discoverable.

  2. Leftover Shape 6 test text boxes -- reported already cleaned up (no
     action taken this session).

  3. Shape 7 (`ListTemplateCollection`/`baseStyle`, Case 75) -- CLOSED.
     Retired the unsafe original plan (manufacturing a violation via
     Modify Style on real content -- the List Paragraph hang trigger) for
     a disposable-blank-document approach instead: `npm run sideload`
     opened a fresh unsaved .docx; a throwaway VBA module in Normal.dotm's
     project (not the blank doc's own -- a .docx can't host macros)
     manufactured both violation types directly via Styles.Add/
     ListTemplates.Add, safe because the hang is paragraph-count scaled
     and a blank doc has none of the triggering list-engine state.
     Two genuine findings surfaced and got corrected live, mid-session:
     (a) Word's own built-in styles (Heading 1/2, Footnote Text, Title)
     already carry a non-empty BaseStyle ("Normal") in a stock document --
     the production docm only reads clean because it was deliberately
     detached to satisfy EDSG/10's rule; (b) the first VBA comparison
     script was scoped to just the 2 manufactured names, silently
     undercounting relative to the JS hook's full 49-name APPROVED_STYLES
     scope -- widened to match. Final live result: VBA count 6 = JS count
     6, exact match. Disposable document closed without saving, throwaway
     module removed, no residue.

  4. Started the 5-item §15.12 research list: Cases 34/35 (footnote
     body-text traversal) PORTED. `Word.NoteItem.body` (WordApi 1.5, not
     desktop-only) was a full API match; reused audit-checks.ts's existing
     `countLinefeeds` (Cases 32/33) completely unchanged -- it already
     scans any string[] independently per element, so one whole-footnote-
     body string per footnote reproduces VBA's exact per-footnote reset
     with zero new pure-logic code. 6 new tests, typecheck + lint clean
     (a pre-existing, unrelated ~200-error lint failure in a stale
     taskpane/dist/ build artifact was found and left alone -- not part
     of this change), 218/218 (taskpane) + 45/45 (root) passing.
     Running total: 58 of ~87.
     NOT YET LIVE-VALIDATED -- new "Shape 9" runbook written into
     word-audit-scanner.LIVE_CHECK.md. This live-check has to confirm two
     things, not one: the new footnote-body-access shape itself, AND a
     pre-existing assumption silently inherited from Cases 32/33 (shipped
     2026-09-19) that was never checked either -- whether Office.js's
     `.text` represents a manual line break (Shift+Enter) as `\v`
     (U+000B), matching VBA's Chr(11). If that assumption turns out
     wrong, Cases 32/33 need the same fix retroactively, not just Shape 9.

NEXT SESSION, IN ORDER:
  1. Run Shape 9's live-check (word-audit-scanner.LIVE_CHECK.md's "Shape 9"
     section, steps 1-6) -- confirms Cases 34/35 AND retroactively settles
     the `\v` question for Cases 32/33. Needs `npm run sideload` again (the
     dev server and debugging session from this session were both stopped
     cleanly at close).
  2. Continue §15.12's remaining 4 research items (page/section-break
     detection 6/80, superscript Find criterion 79, Fields/Bookmarks/
     InlineShapes + dedup 74, multi-type header/footer stories 84) --
     Phase E JS port continuation, running total 58/~87, none blocking.
  3. Case 17's read-only Shape 8 (`countActiveHyperlinks`) still `pending`
     -- low-risk, easy to manufacture whenever convenient (VBA baseline 0,
     document's own editorial rule is zero hyperlinks anywhere).
  4. Shape 5 `rPr` extension (Cases 41/46) still not itself live-validated
     -- low priority, both VBA baselines are 0.
===============================================================================
# Claude Session Manifest -- 2026-09-22
# ------------------------------------------------------------
# All work this session was in aeBibleAddin (JS/.docx port) and
# adaept5tudio (docs/aeBibleClass-word-addin-conversion-plan.md).
# aeBibleClass/src/ was NOT touched this session -- logged here per
# this project's convention of keeping one cross-repo session journal.

## Summary

  Continued §15.12's research queue from the prior session, then ran
  the full backlog of pending live-validation checks accumulated
  across the whole Phase 3 port. By the end of this session:
    - §15.12's research tracker (5 items) is fully closed -- every
      item attempted resolved clean, none a genuine dead end.
    - Every top-level shape and rPr/pPr extension in the port is now
      LIVE-VALIDATED against real Word. Zero open pending-live-
      validation items remain anywhere in the Phase 3 port.
    - Running total: 63 of ~87 (89 actual dispatcher slots; see
      "next session" below) VBA routines ported to aeBibleAddin.

## Files touched this session

  [SCRIPT] aeBibleAddin/taskpane/src/ooxml-run-styles.ts -- extended
  Shape 5's OOXML parser three times: (1) paragraph-level `pPr`
  reading (`pageBreakBefore`, `hostsSectionBreak`, `isEmpty`,
  `hasPageOrColumnBreak`, `startsWithPageOrColumnBreak`) for Cases
  6/80; (2) `OoxmlRunFont.superscript` (`w:vertAlign`) plus
  `countSuperscriptOrdinalSuffixesInParagraph`, the port's first
  cross-run adjacency check, for Case 79.
    STATUS: all live-validated, committed, pushed.

  [SCRIPT] aeBibleAddin/taskpane/src/word-audit-scanner.ts -- added
  `countBareEmptyParagraphsLive`/`countConsecutiveEmptyParagraphsNotPrecedingBreakLive`
  (Cases 6/80), `countNumericOrdinalsLive` (Case 79),
  `countEmptyParagraphsWithInlineContentLive` (Case 74, pure Office.js
  via `body.fields`/`body.getRange().bookmarks`/`body.inlinePictures`
  + `uniqueLocalId` dedup), `countHeaderFooterStyleViolationsLive`
  (Case 84, widens Shape 4's Primary-only pattern to all 3
  header/footer types).
    STATUS: all live-validated, committed, pushed. 277/277 (taskpane)
    + 45/45 (root) passing at session end.

  [SCRIPT] aeBibleAddin/taskpane/src/taskpane.ts -- wired all five new
  functions into `window.aeAuditSpike` as debug hooks.
    STATUS: committed, pushed.

  [TEST] aeBibleAddin/taskpane/src/ooxml-run-styles.test.ts,
  word-audit-scanner.ooxml.test.ts, word-audit-scanner.test.ts --
  62 new tests across the session's five ported Cases.
    STATUS: committed, pushed.

  [DOC] aeBibleAddin/taskpane/src/word-audit-scanner.LIVE_CHECK.md --
  new "Shape 10"/"Shape 11"/"Shape 12"/"Shape 13" runbooks + result
  logs; Shape 8 and the Shape 5 `rPr` extension's long-pending result
  logs finally filled in. Two general findings recorded (see below).
    STATUS: committed, pushed.

  [DOC] adaept5tudio/docs/aeBibleClass-word-addin-conversion-plan.md
  -- §15.12 tracker fully closed; §15.11's stale "Open, not
  investigated further" rows (Cases 34/35, 6/80, 79, 74, 84) found out
  of sync with actual progress and corrected to DONE; all live-check
  closures recorded.
    STATUS: committed, pushed.

## Real findings this session (not just routine porting)

  1. `Range.getOoxml()` throws `RichApi.Error: GeneralException` on a
     COLLAPSED (zero-length) selection -- a blinking cursor isn't
     enough, every OOXML-based debug hook in this file needs an actual
     highlighted range. Discovered mid-Shape-10-live-check.

  2. `Range.getOoxml()` emits a phantom EMPTY `<w:p/>` stub for a
     paragraph a range's end boundary merely TOUCHES without including
     any of its content (e.g. a selection ending exactly at the next
     paragraph's start). `isEmpty`-based checks misclassify that stub
     as bare. Confirmed workaround: extend the range one character
     past the boundary. Applies to every range-scoped OOXML function
     in this port, not just Shape 10 -- worth remembering for any
     future caller scoping to e.g. a chapter boundary.

  3. Case 80's "next paragraph starts with a page/column break"
     exception is structurally unreachable via ordinary Word editing:
     "end of the empty paragraph" and "start of the next paragraph"
     are the same character position, so Ctrl+Enter there always
     attaches the break to the PRECEDING paragraph, never makes it the
     following paragraph's leading content. Same class of dead-code
     finding as Case 6's own already-documented exception. Left
     honestly unexercised rather than forced.

  4. Shape 13's first live-check reading (`12`, not VBA's usual `0`)
     was correctly explained, not a bug: VBA's baseline of 0 only
     holds for the production document's own `TheHeaders`/`TheFooters`
     styles; a scratch document's stock `Header`/`Footer` styles
     legitimately violate every slot (2 sections x 6 types = 12,
     confirmed; re-verified as 6 on a fresh single-section document).

  5. Word's Font dialog "Dark Red" Standard Colors swatch serializes
     as `#C00000` -- a DIFFERENT color from VBA's `wdColorDarkRed`
     constant (`#800000`, per aeBibleClass Test 88), despite sharing a
     display name. Not a parsing bug; worth remembering for anyone
     manufacturing a Case 41/46 test via the Word UI in future (needs
     "More Colors" > custom RGB(128,0,0), not the swatch).

NEXT SESSION:

  No blocking or pending live-validation work remains anywhere in the
  Phase 3 port -- this is a genuine "nothing urgent" state, first time
  since the port began. If continuing the port:

  1. Fresh triage of the ~24 still-untriaged Cases toward the
     dispatcher's actual 89 slots (Tests 1-89; this session's running
     total of 63 covers the ones already resolved one way or another).
     Cases 49/50/51/81 are believed permanently out of scope
     (file-report-writing or, for Case 51, document mutation -- not
     fitting this port's read-only-audit approach); Tests 88/89
     (`AuditColorConstants`, `CountBuiltInHyperlinkStyleRuns`) are
     VBA-side infrastructure checks, not the usual kind of port
     candidate -- worth a deliberate decision on whether they need a
     JS equivalent at all, not just silent exclusion.
  2. No specific research question is queued -- this would be fresh
     triage work, same shape as the original Phase E batches, not a
     continuation of any open thread.
===============================================================================
