# Claude Session Manifest -- 2026-09-17
# ------------------------------------------------------------
# Lists all src/ files modified by Claude this session.
# Use as import checklist after each Claude session.
#
# Import status key:
#   [IMPORT]   - must be imported into VBA editor (Remove old, Import new)
#   [XML]      - ribbon XML only; run py/inject_ribbon.py (no VBA import)
#   [SCRIPT]   - Python/tooling; no VBA import
#   [DOC]      - documentation only; no import
#   [MEMORY]   - Claude auto-memory; no project import
# ------------------------------------------------------------

## Files touched this session (continued - Tests 77/78 fast-fail gate)

  [IMPORT] src/aeBibleClass.cls  (into Blank Bible Copy.docm - operator
           confirmed done via full ImportAllVBAFiles reimport, 38/39
           files, verified against src/ThisDocument.cls content)
    6e17b09    Added a pre-flight gate to
    TheBibleClassTests: a full-suite run takes 15-20+ minutes, but the
    approved-style Priority/UnhideWhenUsed drift that fails Tests 77/78
    (Default Paragraph Font re-surfacing after any run touches it) was
    only ever discovered at Test 77, near the end. New
    CheckApprovedStylesReady() reuses Tests 77/78's own Count functions
    directly (no new detection logic) and halts in seconds instead,
    printing the specific violation + remediation ("run
    WordEditingConfig, save the document, then re-run") to both the
    Immediate window and a MsgBox. Deliberately skipped for single-test
    runs (RUN_THE_TESTS(77) etc.) so Test 77/78 can still be debugged
    directly. Also appended the same remediation text to Tests 77/78's
    own m_lastHint, so the message is identical whether you hit it via
    the fast gate or by letting the full suite run anyway.
    VERIFIED 2026-09-17: operator ran RUN_THE_TESTS against the live
    (still-drifted) docm and got the expected halt message, cleanly
    formatted, in seconds.

## Files touched this session

  [IMPORT] src/aeBibleCitationClass.cls  (into Blank Bible Copy.docm)
    fb321e0    Task 2 (Song of Solomon naming): GetCanonicalBookTable()
               book 22 "Song of Songs" -> "Song of Solomon";
               GetSBLCanonicalBookTable() book 22 "SONG OF SONGS" ->
               "SONG OF SOLOMON"; GetBookAliasMap() adds "SONG OF
               SOLOMON" as an alias (kept "SONG OF SONGS"/"CANTICLES" -
               not removed, only the canonical/display form changed).
               See Plan_scholarly_grounding_2026-09-16.md Task 2.
               STATUS: operator confirmed imported + re-exported; verified
               clean via Run_All_SBL_Tests (44/0 fail) and
               Test_CanonicalNamesAndSBLTable (Song of Solomon rows PASS).

  [IMPORT] src/basTEST_aeBibleCitationClass.bas  (into Blank Bible Copy.docm)
    STATUS: operator confirmed imported (round 2) + re-verified clean -
    see "Verified this session" below for the 361/0 result.
    (a) Test_SongOfSongs_AllAliases - canonical-name assertions now
        expect "Song of Solomon"; added "Song of Solomon" to the alias
        list tested; removed the now-stale negative test that asserted
        "Song of Solomon" was NOT a valid alias (it now is, deliberately);
        kept "Song of Songs" coverage as an alias, not canonical.
    (b) Test_CanonicalNamesAndSBLTable's canonNames(22) updated to match.
    (c) Fixed a pre-existing, unrelated bug found while reading that
        test's output: its book-abbreviation extraction split on the
        FIRST space in ToSBLShortForm's result, truncating every
        multi-word SBL abbreviation ("1 Sam", "2 Cor", "1 John", etc. -
        16 books) to just its leading digit. Now scans for the LAST
        space instead (chapter:verse never contains a space; several
        abbreviations do), mirroring ToSBLShortForm's own last-space
        scan in aeBibleCitationClass.cls.
    (d) Test_CanonicalNamesAndSBLTable wired into Run_All_SBL_Tests
        (previously standalone-only, Alt+F8) - refactored to use the
        suite-level aeAssert instead of its own local aeAssertClass/
        aeLoggerClass, same pattern as the other Stage tests; its
        PROC_ERR handler changed MsgBox -> Debug.Print to match this
        project's error-handling convention. This also fixed Bug B
        below as a side effect (the local aeLoggerClass it used to
        create is gone entirely).

  [IMPORT] aeRibbon/src/aeBibleCitationClass.cls  (into the aeRibbon .dotm -
           SEPARATE VBA project/file from the docm above, see
           project_ribbon_dotm_docx_model.md)
    fb321e0    Same GetCanonicalBookTable()/GetBookAliasMap() changes as
               src/aeBibleCitationClass.cls above. This copy has no
               GetSBLCanonicalBookTable() function, so that part of the
               change doesn't apply here. STATUS UNCONFIRMED - operator
               confirmed the docm side was imported/tested; whether this
               separate .dotm project was also updated is still open.
               No test suite exists in aeRibbon/src to verify it either
               way - manual check needed (type "Song of Solomon"/"Song
               of Songs"/"Solomon" into the ribbon's reference box).

  [SCRIPT] src/basSBL_VerseCountsGenerator.bas,
           aeRibbon/src/basSBL_VerseCountsGenerator.bas
    fb321e0    Cosmetic-only: the "Song of Songs" label passed to
               ToOneBasedLongArray's `context` param (used only in error
               messages, never compared against the canonical table) ->
               "Song of Solomon" for consistency. No behavior change.

  [DOC] rvw/Plan_scholarly_grounding_2026-09-16.md
    91140ba, 3338a9d  Task 2 resolution recorded: WEBU/engwebu_usfm
               evidence, the operator decision, and the 11-step
               implementation checklist (this session is executing it).
    e76ef8d, 5dbed23  Task 1 closed: Zechariah 14:20 inconsistency found
               and fixed (see below).

  [DOC] rvw/Plan_pass3_divine_names_2026-09-15.md
    e76ef8d  Task 1 re-verification note + dated correction bullet for
             Zechariah 14:20 (kept the original 2026-09-15/16 bullet
             as-is per this project's progressive-history convention;
             the correction is a new dated entry, not a rewrite).

  [MEMORY] Claude auto-memory
    project_scholarly_grounding_plan.md, project_rwb_phase4_plan.md,
    MEMORY.md index - kept in sync throughout; no project action needed.

## Non-code changes (docm content, not exportable as a src/ diff)

  Operator made this edit directly in Word, in `Blank Bible Copy.docm`
  (not visible as a git diff outside rpt/docm-verses.txt, but load-
  bearing for the fix above):
    - Zechariah 14:20: "HOLY TO THE LORD" -> "HOLY TO GOD" (inscription
      rendering made consistent with the identical Exodus 28:36/39:30
      inscription - same Hebrew word, same construction, previously
      rendered two different ways with no stated reason).

  Committed as aeBibleClass `e76ef8d` (rpt/docm-verses.txt re-export +
  plan-doc updates), hash filled in via `5dbed23`.

## Verified this session (2026-09-17)

  - `Run_All_SBL_Tests`: **361 tests run, 0 failures.** Confirms Task 2's
    canonical-table/alias-map/test changes, plus two bonus fixes found
    along the way (see basTEST_aeBibleCitationClass.bas entry above):
    (a) `Test_CanonicalNamesAndSBLTable`'s first-space extraction bug
    (16 multi-word SBL abbreviations were false-failing), now fixed and
    wired into the suite; (b) that same test's original standalone
    logger was clobbering `rpt/TestReport.txt` (aeBibleClass.cls's own
    unrelated RUN_THE_TESTS report file) - fixed as a side effect of (a).
  - `RUN_THE_TESTS` (the real, separate aeBibleClass.cls suite):
    re-run by operator to regenerate a correct `rpt/TestReport.txt`
    (confirmed - 87-test format restored, Word build-info footer intact,
    `[2026-09-17 15:56:07] Completed`). Two pre-existing, unrelated
    issues remain in that run, not caused by anything this session:
    Tests 77/78 FAIL, Tests 82/83 hard-skipped (both already tracked
    separately - see `project_docm_verse_export_bug` memory).

## Operator next steps

  1. Confirm whether `aeRibbon.dotm`'s VBA project has also been updated
     with `aeRibbon/src/aeBibleCitationClass.cls` - still open. If not,
     import it there too and manually check the ribbon's reference box
     (no automated test suite exists for that project).
  2. Draft + operator-review + insert the "Song of Songs" editorial
     front-matter note into the docm's Song of Solomon book introduction
     (draft already proposed in-conversation, awaiting operator wording
     approval), then re-run `ExportDocmVersesToRWBFormat` to confirm the
     rwb-format output is unaffected, then close out Task 2's status
     lines (checklist steps 7-11).
  3. Optional, separate from Task 2: Tests 77/78 are FAILing again in
     the just-regenerated `RUN_THE_TESTS` report - this is the same
     recurring regression documented in the 2026-09-16 manifest (fix
     `fcb8172` folded into `PromoteApprovedStyles`, but apparently not
     run/saved since). Not blocking Task 2; flagged here so it isn't
     lost.

## Tests 77/78 gate - full verification + a real incident along the way (2026-09-17)

  - First `RUN_THE_TESTS` after importing the new gate: halted correctly
    in seconds with the expected message (docm still drifted at that
    point) - gate confirmed working.
  - Operator ran `WordEditingConfig`, confirmed `Default Paragraph Font`
    -> Priority 34 in the Immediate window output, then ran
    `RUN_THE_TESTS` again - this run **hung for 30+ minutes with no
    error, no crash dialog, and misleading diagnostics** (no "(Not
    Responding)" title, ~10% CPU, `Run > Break` appeared disabled). The
    one reliable tell: the Immediate Window stopped accepting typed
    input entirely, confirming the main thread was genuinely blocked,
    not just idle-looking.
  - **No safe way to interrupt it** (no physical Break key on this
    keyboard, Break menu unusable, Immediate Window unresponsive) -
    recovered by force-closing `WINWORD.EXE` via Task Manager and
    reopening. Nothing outside that session's unsaved state was lost -
    everything saved/exported earlier in the session (Zechariah 14:20,
    Task 2 code changes) was unaffected. The unsaved `WordEditingConfig`
    fix had to be re-run after reopening.
  - **Working theory for the hang, not independently verified against
    Microsoft docs:** skipping an explicit Debug -> Compile Project
    after the earlier full `ImportAllVBAFiles` reimport left the project
    compiling lazily/incrementally during execution, which can interact
    badly with long-running native calls (`CountDoubleSpaces`'s `Find`
    loop, Test 1, was the one observed stuck). Recorded as a new
    standard workflow step in the
    `feedback_importallvbafiles_error17` memory: always Compile Project
    before running tests after a full reimport.
  - **Second attempt, with Compile Project done first: fully clean.**
    `rpt/TestReport.txt` ([2026-09-17 18:43:32], 897.31s): **85 PASS, 0
    FAIL, 2 SKIP** (Tests 82/83, the pre-existing hard-skip). Tests
    77/78 both PASS. Full suite confirmed working end-to-end with the
    new gate in place.

## New finding while verifying docs before this commit (2026-09-17)

  `rpt/HeadingLog.txt` (regenerated by this session's `RUN_THE_TESTS`
  run) shows the docm's actual printed H1 book-title heading for book 22
  still reads **"SONG OF SONGS"** (`H1[22]`), unchanged - this is
  document content, never touched by Task 2's citation-class code
  changes (steps 1-6), and wasn't in the original 11-step checklist's
  scope at all. By contrast `H1[19]` (Psalms) correctly still reads
  "PSALMS," unaffected, since that was never in question. Added as new
  checklist step 9a in `Plan_scholarly_grounding_2026-09-16.md` -
  requires a manual heading-text edit in `Blank Bible Copy.docm`, same
  as the Zechariah 14:20 fix, not something code can do.

## Session in progress - not yet closed 2026-09-17
