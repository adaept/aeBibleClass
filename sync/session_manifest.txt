# Claude Session Manifest -- 2026-05-28 / 2026-05-30  (FINAL, end of session)
# ------------------------------------------------------------
# Lists all src/ files modified by Claude this session.
# Use as import checklist after each Claude session.
#
# Import status key:
#   [IMPORT]   - must be imported into VBA editor (Remove old, Import new)
#   [XML]      - ribbon XML only; run py/inject_ribbon.py (no VBA import)
#   [SCRIPT]   - Python/tooling; no VBA import
#   [DOC]      - documentation only; no import
#   [MEMORY]   - Claude auto-memory; no project import
# ------------------------------------------------------------

## Session arcs (chronological)

A. INVISIBLE-CHARACTER REDUNDANT-CODE REMOVAL (2026-05-28)
   - Removed redundant `CountInvisibleCharacters` (String return)
     and `TestInvisible` MsgBox harness from
     basTEST_aeBibleConfig.bas. Canonical version lives in
     aeBibleClass.cls:1718 as Test 73 (Long return). No callers
     outside the deleted `TestInvisible` remained.

B. INTRODUCTION STYLE SPEC REALIGN (2026-05-28)
   - Operator updated Introduction style in doc (commit 24ff5a8,
     SpaceBefore 0 -> 12). RUN_TAXONOMY_STYLES flagged FAIL.
   - Spec at basTEST_aeBibleConfig.bas:302 updated dExpSpaceBefore
     0 -> 12. Re-run cleared to PASS.

C. DEFAULT PARAGRAPH FONT PROMOTED TO APPROVED (2026-05-28)
   - DumpAllApprovedStyles surfaced orphan
     style_Default_Paragraph_Font.txt (Priority=2 but not in
     GetApprovedStyles). Operator confirmed promotion was
     deliberate so character styles (EmphasisBlack, EmphasisRed,
     Words of Jesus, BookHyperlink) remain functional inside
     VerseText paragraphs.
   - GetApprovedStyles array grew 48 -> 49 (Default Paragraph
     Font inserted at position 34, before Footnote Reference).
   - AuditOneStyle spec added (Carlito 9, Bold=0 Italic=0,
     BaseStyle="", Color=wdColorAutomatic).
   - WordEditingConfig renumbered priorities; Test 78 PASS at 0;
     RUN_TAXONOMY_STYLES 57 PASS / 0 FAIL.

D. TEST 11 / 33 HINTS (2026-05-30)
   - Test 11: CountFindNumberDashNumber - on first match, sets
     m_lastHint = "page <N> : <match-text>". Was "(no hint
     provided by test function)".
   - Test 33: CountLinefeed - shared by Tests 32/33. Primary loop
     hint capture gated to IsMissing(space) (Test 32 path). New
     second pass in space-prefix branch (Test 33) scans ^l
     matches and records the first whose predecessor is NOT a
     space. Hint shape: page + prev char + +-20-char ctx window.

E. TEST 38 STRUCTURAL CLASSIFICATION + TEST 80 SPLIT (2026-05-30)
   - CountEmptyParagraphs rewritten to emit per-occurrence rows
     to rpt/EmptyParagraphs.txt (TSV with kind column, overwrite
     each run via FSO.CreateTextFile).
   - Classification predicate (matches Word's Layout > Breaks
     labels): SBNP / SBC / SBEP / SBOP / SBNC / PBB / PB / CB /
     Bare. Detection uses AscW(char at para.Range.End) + a
     pre-built secStartPos() -> secStartType() map.
   - First two detection attempts wrong (positional equality
     against Sections(1).Range.End; next-paragraph section index
     comparison). Diagnostic columns revealed this_sec ==
     next_sec for every flagged row and nx_ch=12 universally.
     Correct predicate uses char-after-pilcrow + section start
     position lookup.
   - Test 38 description rewritten to acknowledge that
     essentially all flagged entries are structural carriers,
     not stray pilcrows. Baseline unchanged (216).
   - Test 80 added (CountBareEmptyParagraphs). Isolates the
     truly-bare residual: PBB=False AND next char neither
     Chr(12) nor Chr(14). Expected 0. Standalone walk - no
     ordering dependency on Test 38. Mirrors the Test 22 ->
     22/38/74 split pattern.
   - Wiring: MaxTests 79 -> 80; values array extended with 0;
     GetTestDescription Case 80; GetPassFail Case 80;
     Debug.Print Case 80; BufAppend Case 80.
   - Live-document verification: Test 38 PASS at 216;
     distribution 142 SBNP / 2 PBB / 1 SBC / 0 Bare. Test 80
     PASS at 0.

F. FILE-IO HARDENING (2026-05-30)
   - PROC_ERR in CountEmptyParagraphs closes oStream defensively
     under On Error Resume Next so a mid-run failure does not
     leak a handle across runs.
   - Switched from `Open finalPath For Output As fileNum` to
     FSO.CreateTextFile after hitting Err 55 (file already open,
     from leaked VBA handle) and then Err 70 (permission denied,
     suspected WSL/AV-side share lock). Word restart cleared the
     orphan lock; FSO is now the standing pattern for re-runnable
     report writers - see [[feedback_fso_file_writes]].

## src/ files modified  (require import in this order)

  src/basTEST_aeBibleConfig.bas    [IMPORT]   - Arcs A, B, C
  src/aeBibleClass.cls             [IMPORT]   - Arcs D, E, F

Both files are committed (70cbb94, 2215706, b42523d, 05e1558,
b7f36d3, 41ae1cb, 9352216, 847d302, 8bdbef2) and pushed to
origin/main as of 2026-05-30.

## Non-src/ changes (no VBA import needed)

  rvw/Code_review 2026-05-16.md    [DOC]   - closed with 2026-05-28
                                              session entry
  rvw/Code_review 2026-05-28.md    [DOC]   - new arc opened; carries
                                              forward 9 items;
                                              2026-05-30 dated entry
                                              added for Arcs D-F
  rpt/EmptyParagraphs.txt          [DOC]   - new artefact, written
                                              by Test 38; git-diffable
  rpt/TestReport.txt               [DOC]   - regenerated by RUN_THE_TESTS
  rpt/TotalTimeReport.txt          [DOC]   - regenerated
  rpt/Style Usage Distribution.txt [DOC]   - regenerated
  rpt/HeaderFooterAudit.txt        [DOC]   - regenerated
  rpt/HeadingLog.txt               [DOC]   - regenerated

## Memory updates (personal context, not project-tracked)

  feedback_fso_file_writes.md      [MEMORY] - new entry codifying
                                              the FSO.CreateTextFile
                                              rule and its two failure
                                              modes (Err 55, Err 70)
  MEMORY.md                        [MEMORY] - index entry added

## Operator next steps

  1. (Already done this session.) Tests verified live - 11 hint,
     33 hint, 38 PASS at 216, 80 PASS at 0.
  2. No further import or operator action required for this arc.
  3. Carry-forward open items live in
     rvw/Code_review 2026-05-28.md (9 items: ribbon v1.0.0 build,
     failed-test revisits, date-rule sweep, EDSG Step 0 fix,
     ribbon nav, SoftHyphenSweep sister, SHA i18n, Normal
     audit, class encapsulation rule).
