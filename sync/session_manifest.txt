# Claude Session Manifest -- 2026-05-12
# ------------------------------------------------------------
# Lists all src/ files modified by Claude this session.
# Use as import checklist after each Claude session.
#
# Import status key:
#   [IMPORT]   - must be imported into VBA editor (Remove old, Import new)
#   [XML]      - ribbon XML only; run py/inject_ribbon.py (no VBA import)
#   [SCRIPT]   - Python/tooling; no VBA import
#   [DOC]      - documentation only; no import
#   [DONE]     - already imported into both .docm files this session
# ------------------------------------------------------------

## Session themes

1. Establish aeRibbon/ production export gateway:
   - aeRibbon/src/   - trimmed VBA, call-graph reachable from ribbon XML
   - aeRibbon/template/customUI14.xml
   - aeRibbon/docx/  - host docx instructions (Gate G7)
   - aeRibbon/releases/  - per-version build artefacts
   - aeRibbon/RoutineLog.md, BUILD.md, QA_CHECKLIST.md, RELEASES.md, VERSION
2. md/aeProductionRibbonPlan.md authored and approved (decisions 1-6 in
   plan section 7).
3. py/ribbon_export_trim.py: call-graph trim of ribbon production sources;
   idempotent; preserves VBA lifecycle hooks (Class_Initialize,
   Class_Terminate, AutoExec).
4. Architectural corrections recorded in the plan:
   - aeBibleClass.cls is test infrastructure, not the doc model -
     excluded from aeRibbon/src/.
   - Production end-user document is .docx (code-free, shareable),
     produced manually by Editor/Developer per BUILD.md.

## Files modified

[IMPORT]  src/aeRibbonClass.cls
          - LogHeadingData: added one-line guard at top -
            `If Dir(ActiveDocument.Path & "\rpt", vbDirectory) = "" Then Exit Sub`.
          - Root cause: latent bug. The routine writes a diagnostic CSV
            to ActiveDocument.Path & "\rpt\HeadingLog.txt". In any host
            folder without an rpt\ subfolder, the Open #fNum For Output
            line raised error 76 (Path not found), surfacing as a
            MsgBox at template/docx load. The dev .docm happens to live
            beside C:\adaept\aeBibleClass\rpt so it never triggered.
          - Re-import into the dev .docm files this session.

[IMPORT]  src/Module1.bas
[IMPORT]  src/aeBibleClass.cls
[IMPORT]  src/basAuthorStyles.bas
[IMPORT]  src/basFixDocxRoutines.bas
[IMPORT]  src/basStyleInspector.bas
[IMPORT]  src/basTEST_aeBibleConfig.bas
[IMPORT]  src/basTEST_aeBibleFonts.bas
[IMPORT]  src/basTEST_aeBibleTools.bas
[IMPORT]  src/basWordRepairRunner.bas
          - .Color casing restored (75 replacements across 9 files).
          - Bug: VBE auto-case demoted Font.Color -> Font.color when
            something somewhere was typed lowercase. The normalizer had
            no rule for .Color so the corruption survived.
          - Fix: py/normalize_vba.py - added rule
            (r'(?i)\.Color\b', '.Color', '.Color property on Font/Style/object (Font.Color access)').
          - Re-import into the dev .docm files this session.

[SCRIPT]  py/ribbon_export_trim.py (NEW)
          - Reads ribbon callbacks from customUI14backupRWB.xml as roots.
          - Adds AutoExec, RibbonOnLoad, Document_Open, Instance as
            extra roots, plus VBA lifecycle names always preserved.
          - Parses .bas/.cls files, builds case-insensitive identifier
            call graph, walks reachability, writes trimmed copies to
            aeRibbon/src/.
          - Preserves headers / module-level decls / Type / Enum / Const
            byte-for-byte; only drops whole routine blocks.
          - --check stub (not yet wired).

[DOC]     md/aeProductionRibbonPlan.md (NEW)
          - Production plan; sec 7 records the 6 approved decisions.

[DOC]     aeRibbon/BUILD.md (NEW)
          - Template build steps + manual .docx production procedure.

[DOC]     aeRibbon/QA_CHECKLIST.md (NEW)
          - Gates G1-G8 with sign-off boxes.

[DOC]     aeRibbon/RELEASES.md (NEW)
          - Append-only release ledger; no rows yet.

[DOC]     aeRibbon/VERSION (NEW)
          - 1.0.0+bc71416

[DOC]     aeRibbon/RoutineLog.md (GENERATED by ribbon_export_trim.py)
          - 131 KEPT, 67 REMOVED across 5 files.

[DOC]     aeRibbon/docx/README_host_docx.md (NEW)
          - Manual creation steps for aeRibbon-host.docx (Gate G7).

[XML]     aeRibbon/template/customUI14.xml (NEW)
          - Copy of customUI14backupRWB.xml.

[IMPORT]  aeRibbon/src/basBibleRibbonSetup.bas (GENERATED, 54 routines kept)
[IMPORT]  aeRibbon/src/aeRibbonClass.cls       (GENERATED, 56 kept)
[IMPORT]  aeRibbon/src/aeBibleCitationClass.cls(GENERATED, 12 kept)
[IMPORT]  aeRibbon/src/basRibbonDeferred.bas   (GENERATED, as-is copy)
[IMPORT]  aeRibbon/src/basUIStrings.bas        (GENERATED, as-is copy)

# Note: nothing under src/ was modified this session. The dev sources
# remain the source of truth; aeRibbon/src/ is regenerated by
# py/ribbon_export_trim.py and should not be hand-edited.
